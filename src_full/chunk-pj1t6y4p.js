// Claude Code is a Beta product per Anthropic's Commercial Terms of Service.
// By using Claude Code, you agree that all code acceptance or rejection decisions you make,
// and the associated conversations in context, constitute Feedback under Anthropic's Commercial Terms,
// and may be used to improve Anthropic's products, including training models.
// You are responsible for reviewing any code suggestions before use.

// (c) Anthropic PBC. All rights reserved. Use is subject to the Legal Agreements outlined here: https://code.claude.com/docs/en/legal-and-compliance.

// Version: 2.1.259
import { Twe, ai, Yc, ROe, J6e, rq, fbt, Pr, m7 } from "./chunk-qpwbvc04.js";
import { Vn } from "./chunk-x1rrg5j2.js";
import "./chunk-jdw11prg.js";
import { M } from "./chunk-56nvyfje.js";
import "./chunk-1mrhsd7s.js";
import "./chunk-97tbrkcc.js";
import { be } from "./chunk-kn2qhfka.js";
import { m } from "./chunk-55w4bsdv.js";
import "./chunk-9fysw8jv.js";
import "./chunk-m92n5xra.js";
import "./chunk-m7w5m1w6.js";
import { C, yt, he, Lt } from "./chunk-058caznt.js";
import { Ye, b, Y, t } from "./chunk-fzpv8ev5.js";
import "./chunk-xmrr4sh8.js";
import { le, gt, gn } from "./chunk-ras23w04.js";
import { jIe, h } from "./chunk-6rkpsn9e.js";
import "./chunk-xfn8hpdj.js";
import "./chunk-r8hc3n2z.js";
import "./chunk-ck0tqv1m.js";
import "./chunk-9f9fskgc.js";
import "./chunk-spz20jb6.js";
import "./chunk-616tsvrd.js";
import "./chunk-n8g979s0.js";
import "./chunk-mxy52vze.js";
import "./chunk-zk8esmth.js";
import "./chunk-bx79h7g8.js";
import "./chunk-k1wkanbv.js";
import "./chunk-7y5wjz4e.js";
import "./chunk-vdqz95a3.js";
import "./chunk-5dw4kvcq.js";
import "./chunk-gxyczd8c.js";
import "./chunk-h96shwz8.js";
import { ve } from "./chunk-ptdm1fhw.js";
import "./chunk-gzwhm5vd.js";
import "./chunk-nwzn6gxv.js";
import { Ul, ya } from "./chunk-x722nt0q.js";
import "./chunk-1v541dwj.js";
import "./chunk-8mbwgjdd.js";
import "./chunk-mzmfq60a.js";
import "./chunk-ye42pw2j.js";
import "./chunk-enjww0fp.js";
import "./chunk-0dkpd8qq.js";
import "./chunk-67nd7etf.js";
import "./chunk-7xxnrgeg.js";
import "./chunk-rahwxqh8.js";
import "./chunk-m0a16ehy.js";
import "./chunk-rjxafr3h.js";
import "./chunk-21dppk21.js";
import "./chunk-edmcaynh.js";
import "./chunk-wxd1scze.js";
import "./chunk-77152aqa.js";
import "./chunk-33da912m.js";
import "./chunk-x67fwt53.js";
import "./chunk-8trhjkwe.js";
import "./chunk-4z2eqcrq.js";
import "./chunk-b3pxzdmn.js";
import "./chunk-pwdby7t2.js";
import "./chunk-ta3hhm0z.js";
import "./chunk-a0qeq8pm.js";
import "./chunk-9qgz04yg.js";
import "./chunk-vv5g97a8.js";
import "./chunk-b5ax9mbm.js";
import { ko } from "./chunk-35w62chd.js";
import "./chunk-2yqsfgga.js";
import "./chunk-trntcdrz.js";
import "./chunk-nmde69vm.js";
import "./chunk-7r03n5n9.js";
import "./chunk-9pd12rac.js";
import "./chunk-a7a5sap3.js";
import "./chunk-3r19kwqx.js";
import "./chunk-55t63zqr.js";
import "./chunk-5t2g7ar8.js";
import "./chunk-0xd0k64r.js";
import "./chunk-j2rxdvy0.js";
import "./chunk-qyqph83r.js";
import "./chunk-1qpkzqbm.js";
import "./chunk-nt36bgfs.js";
import "./chunk-6q0mr3fq.js";
import "./chunk-1k9bsfa7.js";
import "./chunk-t387wqyr.js";
import "./chunk-cezzeybd.js";
import "./chunk-dtz7ymrp.js";
import "./chunk-7bh7nxpg.js";
import "./chunk-8nj3fpx9.js";
import "./chunk-wkxp81p6.js";
import { Nl } from "./chunk-zwtg7j89.js";
import "./chunk-npdpr5xp.js";
import "./chunk-f1ybk250.js";
import "./chunk-40xnyyhy.js";
import "./chunk-qjj8swk2.js";
import "./chunk-bbpye6w7.js";
import "./chunk-ksctmzwy.js";
import "./chunk-w75z743m.js";
import "./chunk-30q5ynkm.js";
import "./chunk-mrsbyrhq.js";
import "./chunk-5rcp9861.js";
import "./chunk-dz1fj2ka.js";
import "./chunk-0h1h2vpe.js";
import "./chunk-zj5hwn6k.js";
import "./chunk-xz3v8y7y.js";
import "./chunk-jqjjqb8p.js";
import "./chunk-jtegkk17.js";
import "./chunk-6dcxrmbt.js";
import "./chunk-jkrywj2s.js";
import "./chunk-hnqd864x.js";
import "./chunk-hrhk4q9h.js";
import "./chunk-zw75zfcp.js";
import "./chunk-svmkhj92.js";
import "./chunk-dybqhddz.js";
import "./chunk-31rmjnrq.js";
import "./chunk-q0fs2qb2.js";
import "./chunk-a33zz456.js";
import "./chunk-esn2chqn.js";
import "./chunk-1fhzzx7h.js";
import "./chunk-321z9ran.js";
import "./chunk-vvzgt3y1.js";
import "./chunk-9tnnrncs.js";
import "./chunk-1kx3xrw0.js";
import "./chunk-9fjy6xex.js";
import "./chunk-b4cswg8c.js";
import "./chunk-rnzbwgja.js";
import "./chunk-mq6t5gsy.js";
import "./chunk-0f1244y5.js";
import "./chunk-gbnf6vz0.js";
import "./chunk-11mcqc6q.js";
import "./chunk-m97fmp5a.js";
import "./chunk-sm4f0pvs.js";
import "./chunk-1q5tty9h.js";
import "./chunk-tkq0r7ym.js";
import "./chunk-c5577t1e.js";
import "./chunk-eza32dw3.js";
import "./chunk-5gp3752w.js";
import "./chunk-vpt6j0hj.js";
import "./chunk-bqzk1zdc.js";
import "./chunk-a3w4apvy.js";
import "./chunk-31k5d81d.js";
import "./chunk-s98sa0gn.js";
import "./chunk-yz9ewb10.js";
import "./chunk-meget4pa.js";
import "./chunk-b9h3hrm9.js";
import "./chunk-jrks771b.js";
import "./chunk-y91b9t7k.js";
import "./chunk-t8rh9pbw.js";
import "./chunk-wyqz2xfr.js";
import "./chunk-bzm4f2ge.js";
import "./chunk-7b08b87y.js";
import "./chunk-0697cd2s.js";
import "./chunk-71edvt4b.js";
import "./chunk-yj7edqzj.js";
import "./chunk-bzxfr890.js";
import "./chunk-nh5b9j9c.js";
import "./chunk-qpqhpjnf.js";
import "./chunk-abnm2sa9.js";
import "./chunk-m1g84jsb.js";
import "./chunk-jea0jsvq.js";
import "./chunk-cj0gpwwk.js";
import "./chunk-xxq66zap.js";
import "./chunk-8gvda1c5.js";
import "./chunk-752tnqtv.js";
import "./chunk-6nznq4h0.js";
import "./chunk-075s54dh.js";
import "./chunk-pgmqkm8j.js";
import "./chunk-b3pk8jsy.js";
import "./chunk-67zd0pq0.js";
import "./chunk-13k2svs7.js";
import "./chunk-kxcfgh7d.js";
import "./chunk-f7czbtwe.js";
import "./chunk-grn2cpxb.js";
import "./chunk-s2qv9t07.js";
import "./chunk-rzhk9vzz.js";
import "./chunk-h3nnndyy.js";
import "./chunk-xdnjqx4p.js";
import "./chunk-68asg28d.js";
import "./chunk-at3x5y7v.js";
import "./chunk-wfap5c8g.js";
import "./chunk-4amf009w.js";
import "./chunk-zbjsms1x.js";
import "./chunk-gt0sz7qx.js";
import "./chunk-8cb3kpz7.js";
import { BB } from "./chunk-kvmgr8kf.js";
import "./chunk-gyc9r9jx.js";
import "./chunk-fpm5782j.js";
import "./chunk-g41jh2m0.js";
import "./chunk-mz0qwadn.js";
import "./chunk-eft7b88m.js";
import "./chunk-nzscmy2v.js";
import "./chunk-e4vwkrtj.js";
import "./chunk-6dth79yw.js";
import "./chunk-gj0499cq.js";
import "./chunk-szzsnrz5.js";
import "./chunk-pq20xdar.js";
import "./chunk-h9mewq95.js";
import "./chunk-a3vshzn8.js";
import "./chunk-3e1zwnk7.js";
import "./chunk-7bfx680p.js";
import "./chunk-4796xb9w.js";
import "./chunk-31e4aq9f.js";
import "./chunk-90gearh2.js";
import "./chunk-wtntbnrk.js";
import "./chunk-mh620fe5.js";
import "./chunk-h2yhfh1h.js";
import { _t, ay } from "./chunk-n4c82q7p.js";
import "./chunk-y0c7xssj.js";
import "./chunk-7nw1s99d.js";
import "./chunk-f6ht09n5.js";
import "./chunk-vckrj367.js";
import "./chunk-zc7jwbz1.js";
import "./chunk-50etkfry.js";
import { i, A, O, T, c, ge } from "./chunk-84vc68b7.js";
import "./chunk-c6eb44np.js";
import "./chunk-nc8ww32a.js";
import { tc, Ro } from "./chunk-yggfx0ac.js";
import "./chunk-my1n9ey3.js";
import "./chunk-v10h0yg2.js";
import "./chunk-5m6nm8br.js";
import {
  mkdir as Te,
  readdir as De,
  readFile as Ne,
  unlink as Fe,
  writeFile as de,
} from "fs/promises";
import { extname as He, join as q } from "path";
var Pe = new RegExp(`<(${jIe.join("|")})>[\\s\\S]*?(?:</\\1>|$)`, "g");
function Me(e) {
  return e.replace(Pe, "").trim();
}
function xe(e) {
  if (typeof e === "string") return [{ type: "text", text: e }];
  if (!Array.isArray(e)) return [];
  return e.filter((s) => s !== null && typeof s === "object" && !Array.isArray(s));
}
function ce(e) {
  return xe(e)
    .flatMap((s) => (s.type === "text" && typeof s.text === "string" ? [s.text] : []))
    .map(Me)
    .filter((s) => s !== "");
}
function we(e, s) {
  let o = [],
    r = new Set();
  for (let l of e) {
    if (
      l.type === "attachment" &&
      typeof l.attachment === "object" &&
      l.attachment !== null &&
      fbt(l)
    ) {
      let d = l.attachment.type === "queued_command" ? l.attachment.prompt : "",
        u = l.attachment.source_uuid;
      if (typeof u === "string") {
        if (r.has(u)) continue;
        r.add(u);
      }
      for (let n of ce(d)) o.push(`[User]: ${le(n, s.userTextChars)}`);
      continue;
    }
    if (l.type === "user" && l.message) {
      if (l.isCompactSummary) {
        for (let d of ce(l.message.content))
          o.push(`[Summary of earlier conversation]: ${le(d, s.userTextChars)}`);
        continue;
      }
      if (!rq(l) || (typeof l.uuid === "string" && r.has(l.uuid))) continue;
      for (let d of ce(l.message.content)) o.push(`[User]: ${le(d, s.userTextChars)}`);
    } else if (l.type === "assistant" && l.message) {
      for (let d of xe(l.message.content))
        if (d.type === "text" && typeof d.text === "string")
          o.push(`[Assistant]: ${le(d.text, s.assistantTextChars)}`);
        else if (d.type === "tool_use" && typeof d.name === "string") o.push(`[Tool: ${d.name}]`);
    }
  }
  return o;
}
function $e() {
  return Ul();
}
function Le() {
  return Ul();
}
var Ae = m(() =>
    c({
      session_id: i(),
      transcript_mtime: A().optional(),
      project_path: i(),
      start_time: i(),
      duration_minutes: A(),
      user_message_count: A(),
      assistant_message_count: A(),
      tool_counts: ge(i(), A()).optional(),
      languages: ge(i(), A()).optional(),
      git_commits: A(),
      git_pushes: A(),
      input_tokens: A(),
      output_tokens: A(),
      first_prompt: i(),
      summary: i().optional(),
      user_interruptions: A().optional(),
      user_response_times: T(A()).optional(),
      tool_errors: A().optional(),
      tool_error_categories: ge(i(), A()).optional(),
      uses_task_agent: O().optional(),
      uses_mcp: O().optional(),
      uses_web_search: O().optional(),
      uses_web_fetch: O().optional(),
      lines_added: A().optional(),
      lines_removed: A().optional(),
      files_modified: A().optional(),
      message_hours: T(A()).optional(),
      user_message_timestamps: T(i()).optional(),
    }),
  ),
  Ue = {
    ".ts": "TypeScript",
    ".tsx": "TypeScript",
    ".js": "JavaScript",
    ".jsx": "JavaScript",
    ".py": "Python",
    ".rb": "Ruby",
    ".go": "Go",
    ".rs": "Rust",
    ".java": "Java",
    ".c": "C",
    ".h": "C",
    ".cpp": "C++",
    ".cc": "C++",
    ".cxx": "C++",
    ".hpp": "C++",
    ".hh": "C++",
    ".hxx": "C++",
    ".ipp": "C++",
    ".md": "Markdown",
    ".json": "JSON",
    ".yaml": "YAML",
    ".yml": "YAML",
    ".sh": "Shell",
    ".css": "CSS",
    ".html": "HTML",
  },
  We = {
    debug_investigate: "Debug/Investigate",
    implement_feature: "Implement Feature",
    fix_bug: "Fix Bug",
    write_script_tool: "Write Script/Tool",
    refactor_code: "Refactor Code",
    configure_system: "Configure System",
    create_pr_commit: "Create PR/Commit",
    analyze_data: "Analyze Data",
    understand_codebase: "Understand Codebase",
    write_tests: "Write Tests",
    write_docs: "Write Docs",
    deploy_infra: "Deploy/Infra",
    warmup_minimal: "Cache Warmup",
    fast_accurate_search: "Fast/Accurate Search",
    correct_code_edits: "Correct Code Edits",
    good_explanations: "Good Explanations",
    proactive_help: "Proactive Help",
    multi_file_changes: "Multi-file Changes",
    handled_complexity: "Multi-file Changes",
    good_debugging: "Good Debugging",
    misunderstood_request: "Misunderstood Request",
    wrong_approach: "Wrong Approach",
    buggy_code: "Buggy Code",
    user_rejected_action: "User Rejected Action",
    claude_got_blocked: "Claude Got Blocked",
    user_stopped_early: "User Stopped Early",
    wrong_file_or_location: "Wrong File/Location",
    excessive_changes: "Excessive Changes",
    slow_or_verbose: "Slow/Verbose",
    tool_failed: "Tool Failed",
    user_unclear: "User Unclear",
    external_issue: "External Issue",
    frustrated: "Frustrated",
    dissatisfied: "Dissatisfied",
    likely_satisfied: "Likely Satisfied",
    satisfied: "Satisfied",
    happy: "Happy",
    unsure: "Unsure",
    neutral: "Neutral",
    delighted: "Delighted",
    single_task: "Single Task",
    multi_task: "Multi Task",
    iterative_refinement: "Iterative Refinement",
    exploration: "Exploration",
    quick_question: "Quick Question",
    fully_achieved: "Fully Achieved",
    mostly_achieved: "Mostly Achieved",
    partially_achieved: "Partially Achieved",
    not_achieved: "Not Achieved",
    unclear_from_transcript: "Unclear",
    unhelpful: "Unhelpful",
    slightly_helpful: "Slightly Helpful",
    moderately_helpful: "Moderately Helpful",
    very_helpful: "Very Helpful",
    essential: "Essential",
  };
function oe() {
  return q(be(), "usage-data");
}
function ae() {
  return q(oe(), "facets");
}
function Be() {
  return q(oe(), "session-meta");
}
function ue(e) {
  return ve.userConfigDir("usage-data", ["facets", `${e}.json`]);
}
function Ie(e) {
  return ve.userConfigDir("usage-data", ["session-meta", `${e}.json`]);
}
function Ve(e) {
  return ve.userConfigDir("usage-data", [e]);
}
async function Je(e, s, o) {
  let r = q(oe(), `report-${s}.html`);
  if (M() && o) {
    for (let l of [`report-${s}.html`, "report.html"]) {
      let d = await o.write(Ve(l), e, { mode: 384, keepExistingMode: !0 });
      if (!d.ok)
        throw (
          t(`insights: report write failed: ${Ye(d.error)}`),
          Error("insights: report write through the storage backend failed")
        );
    }
    return r;
  }
  try {
    await Te(oe(), { recursive: !0 });
  } catch {}
  return (
    await de(r, e, { encoding: "utf-8", mode: 384 }),
    await de(q(oe(), "report.html"), e, { encoding: "utf-8", mode: 384 }),
    r
  );
}
function Ge(e) {
  if (e.code !== "Failed") return !1;
  return (
    e.failureClass === "permission" ||
    e.failureClass === "environment" ||
    e.telemetryCode === "UnexpectedAbsent" ||
    e.telemetryCode === "ENOENT"
  );
}
var ke = 50;
function Ce(e, s) {
  let o = [];
  for (let r = 0; r < e.length; r += s) o.push(e.slice(r, r + s));
  return o;
}
function Ee(e) {
  return BB(q(Be(), `${e}.json`), () => Ae().nullable(), {
    defaultValue: null,
    ensureDir: !0,
    indent: 2,
    mode: 384,
  });
}
var qe = `Analyze this Claude Code session and extract structured facets.

CRITICAL GUIDELINES:

1. **goal_categories**: Count ONLY what the USER explicitly asked for.
   - DO NOT count Claude's autonomous codebase exploration
   - DO NOT count work Claude decided to do on its own
   - ONLY count when user says "can you...", "please...", "I need...", "let's..."

2. **user_satisfaction_counts**: Base ONLY on explicit user signals.
   - "Yay!", "great!", "perfect!" \u2192 happy
   - "thanks", "looks good", "that works" \u2192 satisfied
   - "ok, now let's..." (continuing without complaint) \u2192 likely_satisfied
   - "that's not right", "try again" \u2192 dissatisfied
   - "this is broken", "I give up" \u2192 frustrated

3. **friction_counts**: Be specific about what went wrong.
   - misunderstood_request: Claude interpreted incorrectly
   - wrong_approach: Right goal, wrong solution method
   - buggy_code: Code didn't work correctly
   - user_rejected_action: User said no/stop to a tool call
   - excessive_changes: Over-engineered or changed too much

4. If very short or just warmup, use warmup_minimal for goal_category

SESSION:
`;
function se(e) {
  return typeof e === "string" ? e : "";
}
var Ke = [
  [["exit code"], "Command Failed"],
  [["rejected", "doesn't want"], "User Rejected"],
  [["string to replace not found", "no changes"], "Edit Failed"],
  [["modified since read"], "File Changed"],
  [["exceeds maximum", "too large"], "File Too Large"],
  [["file not found", "does not exist"], "File Not Found"],
];
function Xe(e) {
  if (typeof e !== "string") return "Other";
  let s = e.toLowerCase();
  return Ke.find(([o]) => o.some((r) => s.includes(r)))?.[1] ?? "Other";
}
function Qe(e) {
  let s = {},
    o = {},
    r = 0,
    l = 0,
    d = 0,
    u = 0,
    n = 0,
    p = [],
    a = 0,
    f = {},
    x = !1,
    R = 0,
    v = 0,
    j = new Set(),
    z = [],
    L = [],
    F = !1,
    W = !1,
    y = !1,
    P = null;
  for (let U of e.messages) {
    let J = U.timestamp;
    if (U.type === "assistant" && U.message) {
      if (J) P = J;
      let D = U.message.usage;
      if (D) ((d += D.input_tokens || 0), (u += D.output_tokens || 0));
      let B = U.message.content;
      if (Array.isArray(B)) {
        for (let g of B)
          if (g.type === "tool_use" && "name" in g) {
            let k = g.name;
            if (((s[k] = (s[k] || 0) + 1), k === _t || k === ay)) x = !0;
            if (k.startsWith("mcp__")) F = !0;
            if (k === "WebSearch") W = !0;
            if (k === "WebFetch") y = !0;
            let S = g.input;
            if (S) {
              let Q = se(S.file_path);
              if (Q) {
                let H = Ue[He(Q).toLowerCase()];
                if (H) o[H] = (o[H] || 0) + 1;
                if (k === "Edit" || k === "Write") j.add(Q);
              }
              if (k === "Edit") {
                let H = se(S.old_string),
                  K = se(S.new_string);
                for (let Z of Twe(H, K)) {
                  if (Z.added) R += Z.count || 0;
                  if (Z.removed) v += Z.count || 0;
                }
              }
              if (k === "Write") {
                let H = se(S.content);
                if (H)
                  R +=
                    gn(
                      H,
                      `
`,
                    ) + 1;
              }
              let ne = se(S.command);
              if (ne.includes("git commit")) r++;
              if (ne.includes("git push")) l++;
            }
          }
      }
    }
    if (U.type === "user" && U.message) {
      let D = U.message.content,
        B = !1;
      if (typeof D === "string" && D.trim()) B = !0;
      else if (Array.isArray(D)) {
        for (let g of D)
          if (g.type === "text" && "text" in g) {
            B = !0;
            break;
          }
      }
      if (B) {
        if (J) {
          let g = new Date(J);
          if (Number.isFinite(g.getTime())) {
            let k = g.getHours();
            (z.push(k), L.push(J));
          }
        }
        if (P && J) {
          let g = new Date(P).getTime(),
            S = (new Date(J).getTime() - g) / 1000;
          if (S > 2 && S < 3600) p.push(S);
        }
      }
      if (Array.isArray(D)) {
        for (let g of D)
          if (g.type === "tool_result" && "content" in g) {
            if (g.is_error) {
              a++;
              let S = Xe(g.content);
              f[S] = (f[S] || 0) + 1;
            }
          }
      }
      if (typeof D === "string") {
        if (D.includes("[Request interrupted by user")) n++;
      } else if (Array.isArray(D)) {
        for (let g of D)
          if (g.type === "text" && "text" in g && g.text.includes("[Request interrupted by user")) {
            n++;
            break;
          }
      }
    }
  }
  return {
    toolCounts: s,
    languages: o,
    gitCommits: r,
    gitPushes: l,
    inputTokens: d,
    outputTokens: u,
    userInterruptions: n,
    userResponseTimes: p,
    toolErrors: a,
    toolErrorCategories: f,
    usesTaskAgent: x,
    usesMcp: F,
    usesWebSearch: W,
    usesWebFetch: y,
    linesAdded: R,
    linesRemoved: v,
    filesModified: j,
    messageHours: z,
    userMessageTimestamps: L,
  };
}
function me(e) {
  let s = Qe(e),
    o = Yc(e) || "unknown",
    r = e.created.toISOString(),
    l = Math.round((e.modified.getTime() - e.created.getTime()) / 1000 / 60),
    d = 0,
    u = 0;
  for (let n of e.messages) {
    if (n.type === "assistant") u++;
    if (n.type === "user" && n.message) {
      let p = n.message.content,
        a = !1;
      if (typeof p === "string" && p.trim()) a = !0;
      else if (Array.isArray(p)) {
        for (let f of p)
          if (f.type === "text" && "text" in f) {
            a = !0;
            break;
          }
      }
      if (a) d++;
    }
  }
  return {
    session_id: o,
    project_path: e.relocatedCwd ?? e.projectPath ?? "",
    start_time: r,
    duration_minutes: l,
    user_message_count: d,
    assistant_message_count: u,
    tool_counts: s.toolCounts,
    languages: s.languages,
    git_commits: s.gitCommits,
    git_pushes: s.gitPushes,
    input_tokens: s.inputTokens,
    output_tokens: s.outputTokens,
    first_prompt: e.firstPrompt || "",
    summary: e.summary,
    user_interruptions: s.userInterruptions,
    user_response_times: s.userResponseTimes,
    tool_errors: s.toolErrors,
    tool_error_categories: s.toolErrorCategories,
    uses_task_agent: s.usesTaskAgent,
    uses_mcp: s.usesMcp,
    uses_web_search: s.usesWebSearch,
    uses_web_fetch: s.usesWebFetch,
    lines_added: s.linesAdded,
    lines_removed: s.linesRemoved,
    files_modified: s.filesModified.size,
    message_hours: s.messageHours,
    user_message_timestamps: s.userMessageTimestamps,
  };
}
function pe(e, s) {
  if (!s) return !0;
  if (e.user_message_count !== s.user_message_count)
    return e.user_message_count > s.user_message_count;
  return e.duration_minutes > s.duration_minutes;
}
function us(e) {
  let s = new Map();
  for (let o of e) {
    let r = o.meta.session_id;
    if (pe(o.meta, s.get(r)?.meta)) s.set(r, o);
  }
  return [...s.values()];
}
function Ze(e) {
  let s = [],
    o = me(e);
  (s.push(`Session: ${o.session_id.slice(0, 8)}`),
    s.push(`Date: ${o.start_time}`),
    s.push(`Project: ${o.project_path}`),
    s.push(`Duration: ${o.duration_minutes} min`),
    s.push(""));
  for (let r of we(e.messages, { userTextChars: 500, assistantTextChars: 300 })) s.push(r);
  return s.join(`
`);
}
var et = `Summarize this portion of a Claude Code session transcript. Focus on:
1. What the user asked for
2. What Claude did (tools used, files modified)
3. Any friction or issues
4. The outcome

Keep it concise - 3-5 sentences. Preserve specific details like file names, error messages, and user feedback.

TRANSCRIPT CHUNK:
`;
async function tt(e, s) {
  try {
    let o = await m7({
      systemPrompt: ai([]),
      userPrompt: et + e,
      signal: new AbortController().signal,
      options: {
        model: $e(),
        querySource: "insights",
        agents: [],
        isNonInteractiveSession: !0,
        hasAppendSystemPrompt: !1,
        mcpTools: [],
        maxOutputTokensOverride: 500,
        agentContext: ya(),
        credentials: s,
      },
    });
    if (o.isApiErrorMessage) return e.slice(0, 2000);
    return Pr(o.message.content) || e.slice(0, 2000);
  } catch {
    return e.slice(0, 2000);
  }
}
async function st(e, s) {
  let o = Ze(e);
  if (o.length <= 30000) return o;
  let r = 25000,
    l = [];
  for (let p = 0; p < o.length; p += r) l.push(o.slice(p, p + r));
  let d = await Promise.all(l.map((p) => tt(p, s))),
    u = me(e);
  return (
    [
      `Session: ${u.session_id.slice(0, 8)}`,
      `Date: ${u.start_time}`,
      `Project: ${u.project_path}`,
      `Duration: ${u.duration_minutes} min`,
      `[Long session - ${l.length} parts summarized]`,
      "",
    ].join(`
`) +
    d.join(`

---

`)
  );
}
async function ot(e, s) {
  if (s) {
    let [r] = await Re([e], s);
    return r ?? null;
  }
  let o = q(ae(), `${e}.json`);
  try {
    let r = await Ne(o, { encoding: "utf-8" }),
      l = Y(r);
    if (!_e(l)) {
      try {
        await Fe(o);
      } catch {}
      return null;
    }
    return l;
  } catch {
    return null;
  }
}
function Re(e, s) {
  return fe(e, ue, s, async (o, r) => {
    let l;
    try {
      l = Y(Buffer.from(o).toString("utf-8"));
    } catch {
      return null;
    }
    if (!_e(l)) return (await s.delete(ue(r)), null);
    return l;
  });
}
async function nt(e, s) {
  try {
    await Te(ae(), { recursive: !0 });
  } catch {}
  if (M() && s) {
    let r = await s.write(ue(e.session_id), b(e, null, 2), {
      publishDiscipline: "inPlace",
      mode: 384,
    });
    if (!r.ok)
      throw (
        t(`saveFacets: cache write failed: ${Ye(r.error)}`),
        Error("saveFacets: cache write through the storage backend failed")
      );
    return;
  }
  let o = q(ae(), `${e.session_id}.json`);
  await de(o, b(e, null, 2), { encoding: "utf-8", mode: 384 });
}
async function it(e, s) {
  if (s) {
    let [o] = await ze([e], s);
    return o ?? null;
  }
  try {
    let o = await Ee(e).read();
    return o === null ? null : je(o);
  } catch {
    return null;
  }
}
function ze(e, s) {
  return fe(e, Ie, s, (o) => {
    try {
      let r = Ae()
        .nullable()
        .safeParse(Y(Buffer.from(o).toString("utf-8")));
      return r.success && r.data !== null ? je(r.data) : null;
    } catch {
      return null;
    }
  });
}
async function fe(e, s, o, r) {
  if (e.length === 0) return [];
  let l = await o.read(e.map(s));
  if (!l.ok && e.length > 1)
    return (await Promise.all(e.map((u) => fe([u], s, o, r)))).map(([u]) => u ?? null);
  return Promise.all(
    e.map(async (d, u) => {
      let n = l.ok ? l.value.items[u] : void 0;
      return n?.found ? r(n.value, d) : null;
    }),
  );
}
function je(e) {
  return {
    ...e,
    tool_counts: e.tool_counts ?? {},
    languages: e.languages ?? {},
    tool_error_categories: e.tool_error_categories ?? {},
    user_response_times: e.user_response_times ?? [],
    message_hours: e.message_hours ?? [],
    user_message_timestamps: e.user_message_timestamps ?? [],
    user_interruptions: e.user_interruptions ?? 0,
    tool_errors: e.tool_errors ?? 0,
    lines_added: e.lines_added ?? 0,
    lines_removed: e.lines_removed ?? 0,
    files_modified: e.files_modified ?? 0,
    uses_task_agent: e.uses_task_agent ?? !1,
    uses_mcp: e.uses_mcp ?? !1,
    uses_web_search: e.uses_web_search ?? !1,
    uses_web_fetch: e.uses_web_fetch ?? !1,
  };
}
async function rt(e, s) {
  try {
    if (s) {
      let o = await s.write(Ie(e.session_id), b(e, null, 2), { mode: 384 });
      if (!o.ok) {
        let r = o.error;
        if (Ge(r)) t(`saveSessionMeta: cache write failed: ${Ye(r)}`);
        else
          h(
            new C(
              `saveSessionMeta: cache write failed: ${Ye(r)}`,
              "insights session-meta cache write failed",
            ),
          );
      }
      return;
    }
    await Ee(e.session_id).write(e);
  } catch (o) {
    if (Lt(o)) {
      t(`saveSessionMeta: cache write failed: ${o}`);
      return;
    }
    h(o);
  }
}
async function at(e, s, o) {
  try {
    let r = await st(e, o),
      l = `${qe}${r}

RESPOND WITH ONLY A VALID JSON OBJECT matching this schema:
{
  "underlying_goal": "What the user fundamentally wanted to achieve",
  "goal_categories": {"category_name": count, ...},
  "outcome": "fully_achieved|mostly_achieved|partially_achieved|not_achieved|unclear_from_transcript",
  "user_satisfaction_counts": {"level": count, ...},
  "claude_helpfulness": "unhelpful|slightly_helpful|moderately_helpful|very_helpful|essential",
  "session_type": "single_task|multi_task|iterative_refinement|exploration|quick_question",
  "friction_counts": {"friction_type": count, ...},
  "friction_detail": "One sentence describing friction or empty",
  "primary_success": "none|fast_accurate_search|correct_code_edits|good_explanations|proactive_help|multi_file_changes|good_debugging",
  "brief_summary": "One sentence: what user wanted and whether they got it"
}`,
      d = await m7({
        systemPrompt: ai([]),
        userPrompt: l,
        signal: new AbortController().signal,
        options: {
          model: $e(),
          querySource: "insights",
          agents: [],
          isNonInteractiveSession: !0,
          hasAppendSystemPrompt: !1,
          mcpTools: [],
          maxOutputTokensOverride: 4096,
          agentContext: ya(),
          credentials: o,
        },
      }),
      n = Pr(d.message.content).match(/\{[\s\S]*\}/);
    if (!n) return null;
    let p = Y(n[0]);
    if (!_e(p)) return null;
    return { ...p, session_id: s };
  } catch (r) {
    return (t(`Facet extraction failed: ${he(r).message}`, { level: "error" }), null);
  }
}
function ct(e) {
  let o = [];
  for (let p of e)
    for (let a of p.user_message_timestamps) {
      let f = new Date(a).getTime();
      if (!Number.isFinite(f)) continue;
      o.push({ ts: f, sessionId: p.session_id });
    }
  o.sort((p, a) => p.ts - a.ts);
  let r = new Set(),
    l = new Set(),
    d = 0,
    u = new Map();
  for (let p = 0; p < o.length; p++) {
    let a = o[p];
    while (d < p && a.ts - o[d].ts > 1800000) {
      let x = o[d];
      if (u.get(x.sessionId) === d) u.delete(x.sessionId);
      d++;
    }
    let f = u.get(a.sessionId);
    if (f !== void 0)
      for (let x = f + 1; x < p; x++) {
        let R = o[x];
        if (R.sessionId !== a.sessionId) {
          let v = [a.sessionId, R.sessionId].sort().join(":");
          (r.add(v),
            l.add(`${o[f].ts}:${a.sessionId}`),
            l.add(`${R.ts}:${R.sessionId}`),
            l.add(`${a.ts}:${a.sessionId}`));
          break;
        }
      }
    u.set(a.sessionId, p);
  }
  let n = new Set();
  for (let p of r) {
    let [a, f] = p.split(":");
    if (a) n.add(a);
    if (f) n.add(f);
  }
  return { overlap_events: r.size, sessions_involved: n.size, user_messages_during: l.size };
}
function lt(e, s) {
  let o = {
      total_sessions: e.length,
      sessions_with_facets: s.size,
      date_range: { start: "", end: "" },
      total_messages: 0,
      total_duration_hours: 0,
      total_input_tokens: 0,
      total_output_tokens: 0,
      tool_counts: {},
      languages: {},
      git_commits: 0,
      git_pushes: 0,
      projects: {},
      goal_categories: {},
      outcomes: {},
      satisfaction: {},
      helpfulness: {},
      session_types: {},
      friction: {},
      success: {},
      session_summaries: [],
      total_interruptions: 0,
      total_tool_errors: 0,
      tool_error_categories: {},
      user_response_times: [],
      median_response_time: 0,
      avg_response_time: 0,
      sessions_using_task_agent: 0,
      sessions_using_mcp: 0,
      sessions_using_web_search: 0,
      sessions_using_web_fetch: 0,
      total_lines_added: 0,
      total_lines_removed: 0,
      total_files_modified: 0,
      days_active: 0,
      messages_per_day: 0,
      message_hours: [],
      multi_clauding: { overlap_events: 0, sessions_involved: 0, user_messages_during: 0 },
    },
    r = [],
    l = [],
    d = [];
  for (let n of e) {
    (r.push(n.start_time),
      (o.total_messages += n.user_message_count),
      (o.total_duration_hours += n.duration_minutes / 60),
      (o.total_input_tokens += n.input_tokens),
      (o.total_output_tokens += n.output_tokens),
      (o.git_commits += n.git_commits),
      (o.git_pushes += n.git_pushes),
      (o.total_interruptions += n.user_interruptions),
      (o.total_tool_errors += n.tool_errors));
    for (let [a, f] of Object.entries(n.tool_error_categories))
      o.tool_error_categories[a] = (o.tool_error_categories[a] || 0) + f;
    if ((l.push(...n.user_response_times), n.uses_task_agent)) o.sessions_using_task_agent++;
    if (n.uses_mcp) o.sessions_using_mcp++;
    if (n.uses_web_search) o.sessions_using_web_search++;
    if (n.uses_web_fetch) o.sessions_using_web_fetch++;
    ((o.total_lines_added += n.lines_added),
      (o.total_lines_removed += n.lines_removed),
      (o.total_files_modified += n.files_modified),
      d.push(...n.message_hours));
    for (let [a, f] of Object.entries(n.tool_counts))
      o.tool_counts[a] = (o.tool_counts[a] || 0) + f;
    for (let [a, f] of Object.entries(n.languages)) o.languages[a] = (o.languages[a] || 0) + f;
    if (n.project_path) o.projects[n.project_path] = (o.projects[n.project_path] || 0) + 1;
    let p = s.get(n.session_id);
    if (p) {
      for (let [a, f] of ee(p.goal_categories))
        if (f > 0) o.goal_categories[a] = (o.goal_categories[a] || 0) + f;
      o.outcomes[p.outcome] = (o.outcomes[p.outcome] || 0) + 1;
      for (let [a, f] of ee(p.user_satisfaction_counts))
        if (f > 0) o.satisfaction[a] = (o.satisfaction[a] || 0) + f;
      ((o.helpfulness[p.claude_helpfulness] = (o.helpfulness[p.claude_helpfulness] || 0) + 1),
        (o.session_types[p.session_type] = (o.session_types[p.session_type] || 0) + 1));
      for (let [a, f] of ee(p.friction_counts)) if (f > 0) o.friction[a] = (o.friction[a] || 0) + f;
      if (p.primary_success !== "none")
        o.success[p.primary_success] = (o.success[p.primary_success] || 0) + 1;
    }
    if (o.session_summaries.length < 50)
      o.session_summaries.push({
        id: n.session_id.slice(0, 8),
        date: gt(n.start_time, "T"),
        summary: n.summary || n.first_prompt.slice(0, 100),
        goal: p?.underlying_goal,
      });
  }
  if (
    (r.sort(),
    (o.date_range.start = gt(r[0] ?? "", "T")),
    (o.date_range.end = gt(r.at(-1) ?? "", "T")),
    (o.user_response_times = l),
    l.length > 0)
  ) {
    let n = [...l].sort((p, a) => p - a);
    ((o.median_response_time = n[Math.floor(n.length / 2)] || 0),
      (o.avg_response_time = l.reduce((p, a) => p + a, 0) / l.length));
  }
  let u = new Set(r.map((n) => gt(n, "T")));
  return (
    (o.days_active = u.size),
    (o.messages_per_day =
      o.days_active > 0 ? Math.round((o.total_messages / o.days_active) * 10) / 10 : 0),
    (o.message_hours = d),
    (o.multi_clauding = ct(e)),
    o
  );
}
var dt = [
  {
    name: "project_areas",
    prompt: `Analyze this Claude Code usage data and identify project areas.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "areas": [
    {"name": "Area name", "session_count": N, "description": "2-3 sentences about what was worked on and how Claude Code was used."}
  ]
}

Include 4-5 areas. Skip internal CC operations.`,
    maxTokens: 8192,
  },
  {
    name: "interaction_style",
    prompt: `Analyze this Claude Code usage data and describe the user's interaction style.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "narrative": "2-3 paragraphs analyzing HOW the user interacts with Claude Code. Use second person 'you'. Describe patterns: iterate quickly vs detailed upfront specs? Interrupt often or let Claude run? Include specific examples. Use **bold** for key insights.",
  "key_pattern": "One sentence summary of most distinctive interaction style"
}`,
    maxTokens: 8192,
  },
  {
    name: "what_works",
    prompt: `Analyze this Claude Code usage data and identify what's working well for this user. Use second person ("you").

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "intro": "1 sentence of context",
  "impressive_workflows": [
    {"title": "Short title (3-6 words)", "description": "2-3 sentences describing the impressive workflow or approach. Use 'you' not 'the user'."}
  ]
}

Include 3 impressive workflows.`,
    maxTokens: 8192,
  },
  {
    name: "friction_analysis",
    prompt: `Analyze this Claude Code usage data and identify friction points for this user. Use second person ("you").

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "intro": "1 sentence summarizing friction patterns",
  "categories": [
    {"category": "Concrete category name", "description": "1-2 sentences explaining this category and what could be done differently. Use 'you' not 'the user'.", "examples": ["Specific example with consequence", "Another example"]}
  ]
}

Include 3 friction categories with 2 examples each.`,
    maxTokens: 8192,
  },
  {
    name: "suggestions",
    prompt: `Analyze this Claude Code usage data and suggest improvements.

## CC FEATURES REFERENCE (pick from these for features_to_try):
1. **MCP Servers**: Connect Claude to external tools, databases, and APIs via Model Context Protocol.
   - How to use: Run \`claude mcp add <server-name> -- <command>\`
   - Good for: database queries, Slack integration, GitHub issue lookup, connecting to internal APIs

2. **Custom Skills**: Reusable prompts you define as markdown files that run with a single /command.
   - How to use: Create \`.claude/skills/commit/SKILL.md\` with instructions. Then type \`/commit\` to run it.
   - Good for: repetitive workflows - /commit, /review, /test, /deploy, /pr, or complex multi-step workflows

3. **Hooks**: Shell commands that auto-run at specific lifecycle events.
   - How to use: Add to \`.claude/settings.json\` under "hooks" key.
   - Good for: auto-formatting code, running type checks, enforcing conventions

4. **Headless Mode**: Run Claude non-interactively from scripts and CI/CD.
   - How to use: \`claude -p "fix lint errors" --allowedTools "Edit,Read,Bash"\`
   - Good for: CI/CD integration, batch code fixes, automated reviews

5. **Task Agents**: Claude spawns focused subagents for complex exploration or parallel work.
   - How to use: Claude auto-invokes when helpful, or ask "use an agent to explore X"
   - Good for: codebase exploration, understanding complex systems

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "claude_md_additions": [
    {"addition": "A specific line or block to add to CLAUDE.md based on workflow patterns. E.g., 'Always run tests after modifying auth-related files'", "why": "1 sentence explaining why this would help based on actual sessions", "prompt_scaffold": "Instructions for where to add this in CLAUDE.md. E.g., 'Add under ## Testing section'"}
  ],
  "features_to_try": [
    {"feature": "Feature name from CC FEATURES REFERENCE above", "one_liner": "What it does", "why_for_you": "Why this would help YOU based on your sessions", "example_code": "Actual command or config to copy"}
  ],
  "usage_patterns": [
    {"title": "Short title", "suggestion": "1-2 sentence summary", "detail": "3-4 sentences explaining how this applies to YOUR work", "copyable_prompt": "A specific prompt to copy and try"}
  ]
}

IMPORTANT for claude_md_additions: PRIORITIZE instructions that appear MULTIPLE TIMES in the user data. If user told Claude the same thing in 2+ sessions (e.g., 'always run tests', 'use TypeScript'), that's a PRIME candidate - they shouldn't have to repeat themselves.

IMPORTANT for features_to_try: Pick 2-3 from the CC FEATURES REFERENCE above. Include 2-3 items for each category.`,
    maxTokens: 8192,
  },
  {
    name: "on_the_horizon",
    prompt: `Analyze this Claude Code usage data and identify future opportunities.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "intro": "1 sentence about evolving AI-assisted development",
  "opportunities": [
    {"title": "Short title (4-8 words)", "whats_possible": "2-3 ambitious sentences about autonomous workflows", "how_to_try": "1-2 sentences mentioning relevant tooling", "copyable_prompt": "Detailed prompt to try"}
  ]
}

Include 3 opportunities. Think BIG - autonomous workflows, parallel agents, iterating against tests.`,
    maxTokens: 8192,
  },
  ...[],
  {
    name: "fun_ending",
    prompt: `Analyze this Claude Code usage data and find a memorable moment.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "headline": "A memorable QUALITATIVE moment from the transcripts - not a statistic. Something human, funny, or surprising.",
  "detail": "Brief context about when/where this happened"
}

Find something genuinely interesting or amusing from the session summaries.`,
    maxTokens: 8192,
  },
];
async function Se(e, s, o) {
  try {
    let r = await m7({
        systemPrompt: ai([]),
        userPrompt:
          e.prompt +
          `

DATA:
` +
          s,
        signal: new AbortController().signal,
        options: {
          model: Le(),
          querySource: "insights",
          agents: [],
          isNonInteractiveSession: !0,
          hasAppendSystemPrompt: !1,
          mcpTools: [],
          maxOutputTokensOverride: e.maxTokens,
          agentContext: ya(),
          credentials: o,
        },
      }),
      l = Pr(r.message.content);
    if (l) {
      let d = l.match(/\{[\s\S]*\}/);
      if (d)
        try {
          return { name: e.name, result: Y(d[0]) };
        } catch {
          return { name: e.name, result: null };
        }
    }
    return { name: e.name, result: null };
  } catch (r) {
    return (
      h(yt(Error(`${e.name} failed: ${he(r).message}`), "insight section query failed")),
      { name: e.name, result: null }
    );
  }
}
async function ut(e, s, o) {
  let r = Array.from(s.values())
      .slice(0, 50)
      .map((y) => `- ${y.brief_summary} (${y.outcome}, ${y.claude_helpfulness})`).join(`
`),
    l = Array.from(s.values())
      .filter((y) => y.friction_detail)
      .slice(0, 20)
      .map((y) => `- ${y.friction_detail}`).join(`
`),
    d = Array.from(s.values())
      .flatMap((y) => y.user_instructions_to_claude || [])
      .slice(0, 15)
      .map((y) => `- ${y}`).join(`
`),
    n =
      b(
        {
          sessions: e.total_sessions,
          analyzed: e.sessions_with_facets,
          date_range: e.date_range,
          messages: e.total_messages,
          hours: Math.round(e.total_duration_hours),
          commits: e.git_commits,
          top_tools: Object.entries(e.tool_counts)
            .sort((y, P) => P[1] - y[1])
            .slice(0, 8),
          top_goals: Object.entries(e.goal_categories)
            .sort((y, P) => P[1] - y[1])
            .slice(0, 8),
          outcomes: e.outcomes,
          satisfaction: e.satisfaction,
          friction: e.friction,
          success: e.success,
          languages: e.languages,
        },
        null,
        2,
      ) +
      `

SESSION SUMMARIES:
` +
      r +
      `

FRICTION DETAILS:
` +
      l +
      `

USER INSTRUCTIONS TO CLAUDE:
` +
      (d || "None captured"),
    p = await Promise.all(dt.map((y) => Se(y, n, o))),
    a = {};
  for (let { name: y, result: P } of p) if (P) a[y] = P;
  let f =
      a.project_areas?.areas?.map((y) => `- ${y.name}: ${y.description}`).join(`
`) || "",
    x =
      a.what_works?.impressive_workflows?.map((y) => `- ${y.title}: ${y.description}`).join(`
`) || "",
    R =
      a.friction_analysis?.categories?.map((y) => `- ${y.category}: ${y.description}`).join(`
`) || "",
    v =
      a.suggestions?.features_to_try?.map((y) => `- ${y.feature}: ${y.one_liner}`).join(`
`) || "",
    j =
      a.suggestions?.usage_patterns?.map((y) => `- ${y.title}: ${y.suggestion}`).join(`
`) || "",
    z =
      a.on_the_horizon?.opportunities?.map((y) => `- ${y.title}: ${y.whats_possible}`).join(`
`) || "",
    F = {
      name: "at_a_glance",
      prompt: `You're writing an "At a Glance" summary for a Claude Code usage insights report for Claude Code users. The goal is to help them understand their usage and improve how they can use Claude better, especially as models improve.

Use this 4-part structure:

1. **What's working** - What is the user's unique style of interacting with Claude and what are some impactful things they've done? You can include one or two details, but keep it high level since things might not be fresh in the user's memory. Don't be fluffy or overly complimentary. Also, don't focus on the tool calls they use.

2. **What's hindering you** - Split into (a) Claude's fault (misunderstandings, wrong approaches, bugs) and (b) user-side friction (not providing enough context, environment issues -- ideally more general than just one project). Be honest but constructive.

3. **Quick wins to try** - Specific Claude Code features they could try from the examples below, or a workflow technique if you think it's really compelling. (Avoid stuff like "Ask Claude to confirm before taking actions" or "Type out more context up front" which are less compelling.)

4. **Ambitious workflows for better models** - As we move to much more capable models over the next 3-6 months, what should they prepare for? What workflows that seem impossible now will become possible? Draw from the appropriate section below.

Keep each section to 2-3 not-too-long sentences. Don't overwhelm the user. Don't mention specific numerical stats or underlined_categories from the session data below. Use a coaching tone.

RESPOND WITH ONLY A VALID JSON OBJECT:
{
  "whats_working": "(refer to instructions above)",
  "whats_hindering": "(refer to instructions above)",
  "quick_wins": "(refer to instructions above)",
  "ambitious_workflows": "(refer to instructions above)"
}

SESSION DATA:
${n}

## Project Areas (what user works on)
${f}

## Big Wins (impressive accomplishments)
${x}

## Friction Categories (where things go wrong)
${R}

## Features to Try
${v}

## Usage Patterns to Adopt
${j}

## On the Horizon (ambitious workflows for better models)
${z}`,
      maxTokens: 8192,
    },
    W = await Se(F, "", o);
  if (W.result) a.at_a_glance = W.result;
  return a;
}
function re(e) {
  return ko(e).replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>");
}
var pt = ["frustrated", "dissatisfied", "likely_satisfied", "satisfied", "happy", "unsure"],
  mt = [
    "not_achieved",
    "partially_achieved",
    "mostly_achieved",
    "fully_achieved",
    "unclear_from_transcript",
  ];
function G(e, s, o = 6, r) {
  let l;
  if (r) l = r.filter((u) => u in e && (e[u] ?? 0) > 0).map((u) => [u, e[u] ?? 0]);
  else
    l = Object.entries(e)
      .sort((u, n) => n[1] - u[1])
      .slice(0, o);
  if (l.length === 0) return '<p class="empty">No data</p>';
  let d = Math.max(...l.map((u) => u[1]));
  return l.map(([u, n]) => {
    let p = (n / d) * 100,
      a = We[u] || u.replaceAll("_", " ").replace(/\b\w/g, (f) => f.toUpperCase());
    return `<div class="bar-row">
        <div class="bar-label">${ko(a)}</div>
        <div class="bar-track"><div class="bar-fill" style="width:${p}%;background:${s}"></div></div>
        <div class="bar-value">${n}</div>
      </div>`;
  }).join(`
`);
}
function ft(e) {
  if (e.length === 0) return '<p class="empty">No response time data</p>';
  let s = { "2-10s": 0, "10-30s": 0, "30s-1m": 0, "1-2m": 0, "2-5m": 0, "5-15m": 0, ">15m": 0 };
  for (let r of e)
    if (r < 10) s["2-10s"] = (s["2-10s"] ?? 0) + 1;
    else if (r < 30) s["10-30s"] = (s["10-30s"] ?? 0) + 1;
    else if (r < 60) s["30s-1m"] = (s["30s-1m"] ?? 0) + 1;
    else if (r < 120) s["1-2m"] = (s["1-2m"] ?? 0) + 1;
    else if (r < 300) s["2-5m"] = (s["2-5m"] ?? 0) + 1;
    else if (r < 900) s["5-15m"] = (s["5-15m"] ?? 0) + 1;
    else s[">15m"] = (s[">15m"] ?? 0) + 1;
  let o = Math.max(...Object.values(s));
  if (o === 0) return '<p class="empty">No response time data</p>';
  return Object.entries(s).map(([r, l]) => {
    let d = (l / o) * 100;
    return `<div class="bar-row">
        <div class="bar-label">${r}</div>
        <div class="bar-track"><div class="bar-fill" style="width:${d}%;background:#6366f1"></div></div>
        <div class="bar-value">${l}</div>
      </div>`;
  }).join(`
`);
}
function ht(e) {
  if (e.length === 0) return '<p class="empty">No time data</p>';
  let s = [
      { label: "Morning (6-12)", range: [6, 7, 8, 9, 10, 11] },
      { label: "Afternoon (12-18)", range: [12, 13, 14, 15, 16, 17] },
      { label: "Evening (18-24)", range: [18, 19, 20, 21, 22, 23] },
      { label: "Night (0-6)", range: [0, 1, 2, 3, 4, 5] },
    ],
    o = {};
  for (let u of e) o[u] = (o[u] || 0) + 1;
  let r = s.map((u) => ({ label: u.label, count: u.range.reduce((n, p) => n + (o[p] || 0), 0) })),
    l = Math.max(...r.map((u) => u.count)) || 1;
  return `<div id="hour-histogram">${r.map(
    (u) => `
      <div class="bar-row">
        <div class="bar-label">${u.label}</div>
        <div class="bar-track"><div class="bar-fill" style="width:${(u.count / l) * 100}%;background:#8b5cf6"></div></div>
        <div class="bar-value">${u.count}</div>
      </div>`,
  ).join(`
`)}</div>`;
}
function bt(e) {
  let s = {};
  for (let o of e) s[o] = (s[o] || 0) + 1;
  return b(s);
}
function vt(e, s) {
  let o = (g) => {
      if (!g) return "";
      return g
        .split(
          `

`,
        )
        .map((k) => {
          let S = ko(k);
          return (
            (S = S.replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>")),
            (S = S.replace(/^- /gm, "\u2022 ")),
            (S = S.replaceAll(
              `
`,
              "<br>",
            )),
            `<p>${S}</p>`
          );
        }).join(`
`);
    },
    r = s.at_a_glance,
    l = r
      ? `
    <div class="at-a-glance">
      <div class="glance-title">At a Glance</div>
      <div class="glance-sections">
        ${r.whats_working ? `<div class="glance-section"><strong>What's working:</strong> ${re(r.whats_working)} <a href="#section-wins" class="see-more">Impressive Things You Did \u2192</a></div>` : ""}
        ${r.whats_hindering ? `<div class="glance-section"><strong>What's hindering you:</strong> ${re(r.whats_hindering)} <a href="#section-friction" class="see-more">Where Things Go Wrong \u2192</a></div>` : ""}
        ${r.quick_wins ? `<div class="glance-section"><strong>Quick wins to try:</strong> ${re(r.quick_wins)} <a href="#section-features" class="see-more">Features to Try \u2192</a></div>` : ""}
        ${r.ambitious_workflows ? `<div class="glance-section"><strong>Ambitious workflows:</strong> ${re(r.ambitious_workflows)} <a href="#section-horizon" class="see-more">On the Horizon \u2192</a></div>` : ""}
      </div>
    </div>
    `
      : "",
    d = s.project_areas?.areas || [],
    u =
      d.length > 0
        ? `
    <h2 id="section-work">What You Work On</h2>
    <div class="project-areas">
      ${d
        .map(
          (g) => `
        <div class="project-area">
          <div class="area-header">
            <span class="area-name">${ko(g.name)}</span>
            <span class="area-count">~${g.session_count} sessions</span>
          </div>
          <div class="area-desc">${ko(g.description)}</div>
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : "",
    n = s.interaction_style,
    p = n?.narrative
      ? `
    <h2 id="section-usage">How You Use Claude Code</h2>
    <div class="narrative">
      ${o(n.narrative)}
      ${n.key_pattern ? `<div class="key-insight"><strong>Key pattern:</strong> ${ko(n.key_pattern)}</div>` : ""}
    </div>
    `
      : "",
    a = s.what_works,
    f =
      a?.impressive_workflows && a.impressive_workflows.length > 0
        ? `
    <h2 id="section-wins">Impressive Things You Did</h2>
    ${a.intro ? `<p class="section-intro">${ko(a.intro)}</p>` : ""}
    <div class="big-wins">
      ${a.impressive_workflows
        .map(
          (g) => `
        <div class="big-win">
          <div class="big-win-title">${ko(g.title || "")}</div>
          <div class="big-win-desc">${ko(g.description || "")}</div>
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : "",
    x = s.friction_analysis,
    R =
      x?.categories && x.categories.length > 0
        ? `
    <h2 id="section-friction">Where Things Go Wrong</h2>
    ${x.intro ? `<p class="section-intro">${ko(x.intro)}</p>` : ""}
    <div class="friction-categories">
      ${x.categories
        .map(
          (g) => `
        <div class="friction-category">
          <div class="friction-title">${ko(g.category || "")}</div>
          <div class="friction-desc">${ko(g.description || "")}</div>
          ${g.examples ? `<ul class="friction-examples">${g.examples.map((k) => `<li>${ko(k)}</li>`).join("")}</ul>` : ""}
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : "",
    v = s.suggestions,
    j = v
      ? `
    ${
      v.claude_md_additions && v.claude_md_additions.length > 0
        ? `
    <h2 id="section-features">Existing CC Features to Try</h2>
    <div class="claude-md-section">
      <h3>Suggested CLAUDE.md Additions</h3>
      <p style="font-size: 12px; color: #64748b; margin-bottom: 12px;">Just copy this into Claude Code to add it to your CLAUDE.md.</p>
      <div class="claude-md-actions">
        <button class="copy-all-btn" onclick="copyAllCheckedClaudeMd()">Copy All Checked</button>
      </div>
      ${v.claude_md_additions
        .map(
          (g, k) => `
        <div class="claude-md-item">
          <input type="checkbox" id="cmd-${k}" class="cmd-checkbox" checked data-text="${ko(g.prompt_scaffold || g.where || "Add to CLAUDE.md")}\\n\\n${ko(g.addition)}">
          <label for="cmd-${k}">
            <code class="cmd-code">${ko(g.addition)}</code>
            <button class="copy-btn" onclick="copyCmdItem(${k})">Copy</button>
          </label>
          <div class="cmd-why">${ko(g.why)}</div>
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : ""
    }
    ${
      v.features_to_try && v.features_to_try.length > 0
        ? `
    <p style="font-size: 13px; color: #64748b; margin-bottom: 12px;">Just copy this into Claude Code and it'll set it up for you.</p>
    <div class="features-section">
      ${v.features_to_try
        .map(
          (g) => `
        <div class="feature-card">
          <div class="feature-title">${ko(g.feature || "")}</div>
          <div class="feature-oneliner">${ko(g.one_liner || "")}</div>
          <div class="feature-why"><strong>Why for you:</strong> ${ko(g.why_for_you || "")}</div>
          ${
            g.example_code
              ? `
          <div class="feature-examples">
            <div class="feature-example">
              <div class="example-code-row">
                <code class="example-code">${ko(g.example_code)}</code>
                <button class="copy-btn" onclick="copyText(this)">Copy</button>
              </div>
            </div>
          </div>
          `
              : ""
          }
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : ""
    }
    ${
      v.usage_patterns && v.usage_patterns.length > 0
        ? `
    <h2 id="section-patterns">New Ways to Use Claude Code</h2>
    <p style="font-size: 13px; color: #64748b; margin-bottom: 12px;">Just copy this into Claude Code and it'll walk you through it.</p>
    <div class="patterns-section">
      ${v.usage_patterns
        .map(
          (g) => `
        <div class="pattern-card">
          <div class="pattern-title">${ko(g.title || "")}</div>
          <div class="pattern-summary">${ko(g.suggestion || "")}</div>
          ${g.detail ? `<div class="pattern-detail">${ko(g.detail)}</div>` : ""}
          ${
            g.copyable_prompt
              ? `
          <div class="copyable-prompt-section">
            <div class="prompt-label">Paste into Claude Code:</div>
            <div class="copyable-prompt-row">
              <code class="copyable-prompt">${ko(g.copyable_prompt)}</code>
              <button class="copy-btn" onclick="copyText(this)">Copy</button>
            </div>
          </div>
          `
              : ""
          }
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : ""
    }
    `
      : "",
    z = s.on_the_horizon,
    L =
      z?.opportunities && z.opportunities.length > 0
        ? `
    <h2 id="section-horizon">On the Horizon</h2>
    ${z.intro ? `<p class="section-intro">${ko(z.intro)}</p>` : ""}
    <div class="horizon-section">
      ${z.opportunities
        .map(
          (g) => `
        <div class="horizon-card">
          <div class="horizon-title">${ko(g.title || "")}</div>
          <div class="horizon-possible">${ko(g.whats_possible || "")}</div>
          ${g.how_to_try ? `<div class="horizon-tip"><strong>Getting started:</strong> ${ko(g.how_to_try)}</div>` : ""}
          ${g.copyable_prompt ? `<div class="pattern-prompt"><div class="prompt-label">Paste into Claude Code:</div><code>${ko(g.copyable_prompt)}</code><button class="copy-btn" onclick="copyText(this)">Copy</button></div>` : ""}
        </div>
      `,
        )
        .join("")}
    </div>
    `
        : "",
    F = [],
    W = [],
    y =
      F.length > 0 || W.length > 0
        ? `
    <h2 id="section-feedback" class="feedback-header">Closing the Loop: Feedback for Other Teams</h2>
    <p class="feedback-intro">Suggestions for the CC product and model teams based on your usage patterns. Click to expand.</p>
    ${
      F.length > 0
        ? `
    <div class="collapsible-section">
      <div class="collapsible-header" onclick="toggleCollapsible(this)">
        <span class="collapsible-arrow">\u25B6</span>
        <h3>Product Improvements for CC Team</h3>
      </div>
      <div class="collapsible-content">
        <div class="suggestions-section">
          ${F.map(
            (g) => `
            <div class="feedback-card team-card">
              <div class="feedback-title">${ko(g.title || "")}</div>
              <div class="feedback-detail">${ko(g.detail || "")}</div>
              ${g.evidence ? `<div class="feedback-evidence"><em>Evidence:</em> ${ko(g.evidence)}</div>` : ""}
            </div>
          `,
          ).join("")}
        </div>
      </div>
    </div>
    `
        : ""
    }
    ${
      W.length > 0
        ? `
    <div class="collapsible-section">
      <div class="collapsible-header" onclick="toggleCollapsible(this)">
        <span class="collapsible-arrow">\u25B6</span>
        <h3>Model Behavior Improvements</h3>
      </div>
      <div class="collapsible-content">
        <div class="suggestions-section">
          ${W.map(
            (g) => `
            <div class="feedback-card model-card">
              <div class="feedback-title">${ko(g.title || "")}</div>
              <div class="feedback-detail">${ko(g.detail || "")}</div>
              ${g.evidence ? `<div class="feedback-evidence"><em>Evidence:</em> ${ko(g.evidence)}</div>` : ""}
            </div>
          `,
          ).join("")}
        </div>
      </div>
    </div>
    `
        : ""
    }
    `
        : "",
    P = s.fun_ending,
    U = P?.headline
      ? `
    <div class="fun-ending">
      <div class="fun-headline">"${ko(P.headline)}"</div>
      ${P.detail ? `<div class="fun-detail">${ko(P.detail)}</div>` : ""}
    </div>
    `
      : "",
    J = `
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; background: #f8fafc; color: #334155; line-height: 1.65; padding: 48px 24px; }
    .container { max-width: 800px; margin: 0 auto; }
    h1 { font-size: 32px; font-weight: 700; color: #0f172a; margin-bottom: 8px; }
    h2 { font-size: 20px; font-weight: 600; color: #0f172a; margin-top: 48px; margin-bottom: 16px; }
    .subtitle { color: #64748b; font-size: 15px; margin-bottom: 32px; }
    .nav-toc { display: flex; flex-wrap: wrap; gap: 8px; margin: 24px 0 32px 0; padding: 16px; background: white; border-radius: 8px; border: 1px solid #e2e8f0; }
    .nav-toc a { font-size: 12px; color: #64748b; text-decoration: none; padding: 6px 12px; border-radius: 6px; background: #f1f5f9; transition: all 0.15s; }
    .nav-toc a:hover { background: #e2e8f0; color: #334155; }
    .stats-row { display: flex; gap: 24px; margin-bottom: 40px; padding: 20px 0; border-top: 1px solid #e2e8f0; border-bottom: 1px solid #e2e8f0; flex-wrap: wrap; }
    .stat { text-align: center; }
    .stat-value { font-size: 24px; font-weight: 700; color: #0f172a; }
    .stat-label { font-size: 11px; color: #64748b; text-transform: uppercase; }
    .at-a-glance { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border: 1px solid #f59e0b; border-radius: 12px; padding: 20px 24px; margin-bottom: 32px; }
    .glance-title { font-size: 16px; font-weight: 700; color: #92400e; margin-bottom: 16px; }
    .glance-sections { display: flex; flex-direction: column; gap: 12px; }
    .glance-section { font-size: 14px; color: #78350f; line-height: 1.6; }
    .glance-section strong { color: #92400e; }
    .see-more { color: #b45309; text-decoration: none; font-size: 13px; white-space: nowrap; }
    .see-more:hover { text-decoration: underline; }
    .project-areas { display: flex; flex-direction: column; gap: 12px; margin-bottom: 32px; }
    .project-area { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; }
    .area-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
    .area-name { font-weight: 600; font-size: 15px; color: #0f172a; }
    .area-count { font-size: 12px; color: #64748b; background: #f1f5f9; padding: 2px 8px; border-radius: 4px; }
    .area-desc { font-size: 14px; color: #475569; line-height: 1.5; }
    .narrative { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 20px; margin-bottom: 24px; }
    .narrative p { margin-bottom: 12px; font-size: 14px; color: #475569; line-height: 1.7; }
    .key-insight { background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 12px 16px; margin-top: 12px; font-size: 14px; color: #166534; }
    .section-intro { font-size: 14px; color: #64748b; margin-bottom: 16px; }
    .big-wins { display: flex; flex-direction: column; gap: 12px; margin-bottom: 24px; }
    .big-win { background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 16px; }
    .big-win-title { font-weight: 600; font-size: 15px; color: #166534; margin-bottom: 8px; }
    .big-win-desc { font-size: 14px; color: #15803d; line-height: 1.5; }
    .friction-categories { display: flex; flex-direction: column; gap: 16px; margin-bottom: 24px; }
    .friction-category { background: #fef2f2; border: 1px solid #fca5a5; border-radius: 8px; padding: 16px; }
    .friction-title { font-weight: 600; font-size: 15px; color: #991b1b; margin-bottom: 6px; }
    .friction-desc { font-size: 13px; color: #7f1d1d; margin-bottom: 10px; }
    .friction-examples { margin: 0 0 0 20px; font-size: 13px; color: #334155; }
    .friction-examples li { margin-bottom: 4px; }
    .claude-md-section { background: #eff6ff; border: 1px solid #bfdbfe; border-radius: 8px; padding: 16px; margin-bottom: 20px; }
    .claude-md-section h3 { font-size: 14px; font-weight: 600; color: #1e40af; margin: 0 0 12px 0; }
    .claude-md-actions { margin-bottom: 12px; padding-bottom: 12px; border-bottom: 1px solid #dbeafe; }
    .copy-all-btn { background: #2563eb; color: white; border: none; border-radius: 4px; padding: 6px 12px; font-size: 12px; cursor: pointer; font-weight: 500; transition: all 0.2s; }
    .copy-all-btn:hover { background: #1d4ed8; }
    .copy-all-btn.copied { background: #16a34a; }
    .claude-md-item { display: flex; flex-wrap: wrap; align-items: flex-start; gap: 8px; padding: 10px 0; border-bottom: 1px solid #dbeafe; }
    .claude-md-item:last-child { border-bottom: none; }
    .cmd-checkbox { margin-top: 2px; }
    .cmd-code { background: white; padding: 8px 12px; border-radius: 4px; font-size: 12px; color: #1e40af; border: 1px solid #bfdbfe; font-family: monospace; display: block; white-space: pre-wrap; word-break: break-word; flex: 1; }
    .cmd-why { font-size: 12px; color: #64748b; width: 100%; padding-left: 24px; margin-top: 4px; }
    .features-section, .patterns-section { display: flex; flex-direction: column; gap: 12px; margin: 16px 0; }
    .feature-card { background: #f0fdf4; border: 1px solid #86efac; border-radius: 8px; padding: 16px; }
    .pattern-card { background: #f0f9ff; border: 1px solid #7dd3fc; border-radius: 8px; padding: 16px; }
    .feature-title, .pattern-title { font-weight: 600; font-size: 15px; color: #0f172a; margin-bottom: 6px; }
    .feature-oneliner { font-size: 14px; color: #475569; margin-bottom: 8px; }
    .pattern-summary { font-size: 14px; color: #475569; margin-bottom: 8px; }
    .feature-why, .pattern-detail { font-size: 13px; color: #334155; line-height: 1.5; }
    .feature-examples { margin-top: 12px; }
    .feature-example { padding: 8px 0; border-top: 1px solid #d1fae5; }
    .feature-example:first-child { border-top: none; }
    .example-desc { font-size: 13px; color: #334155; margin-bottom: 6px; }
    .example-code-row { display: flex; align-items: flex-start; gap: 8px; }
    .example-code { flex: 1; background: #f1f5f9; padding: 8px 12px; border-radius: 4px; font-family: monospace; font-size: 12px; color: #334155; overflow-x: auto; white-space: pre-wrap; }
    .copyable-prompt-section { margin-top: 12px; padding-top: 12px; border-top: 1px solid #e2e8f0; }
    .copyable-prompt-row { display: flex; align-items: flex-start; gap: 8px; }
    .copyable-prompt { flex: 1; background: #f8fafc; padding: 10px 12px; border-radius: 4px; font-family: monospace; font-size: 12px; color: #334155; border: 1px solid #e2e8f0; white-space: pre-wrap; line-height: 1.5; }
    .feature-code { background: #f8fafc; padding: 12px; border-radius: 6px; margin-top: 12px; border: 1px solid #e2e8f0; display: flex; align-items: flex-start; gap: 8px; }
    .feature-code code { flex: 1; font-family: monospace; font-size: 12px; color: #334155; white-space: pre-wrap; }
    .pattern-prompt { background: #f8fafc; padding: 12px; border-radius: 6px; margin-top: 12px; border: 1px solid #e2e8f0; }
    .pattern-prompt code { font-family: monospace; font-size: 12px; color: #334155; display: block; white-space: pre-wrap; margin-bottom: 8px; }
    .prompt-label { font-size: 11px; font-weight: 600; text-transform: uppercase; color: #64748b; margin-bottom: 6px; }
    .copy-btn { background: #e2e8f0; border: none; border-radius: 4px; padding: 4px 8px; font-size: 11px; cursor: pointer; color: #475569; flex-shrink: 0; }
    .copy-btn:hover { background: #cbd5e1; }
    .charts-row { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin: 24px 0; }
    .chart-card { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; }
    .chart-title { font-size: 12px; font-weight: 600; color: #64748b; text-transform: uppercase; margin-bottom: 12px; }
    .bar-row { display: flex; align-items: center; margin-bottom: 6px; }
    .bar-label { width: 100px; font-size: 11px; color: #475569; flex-shrink: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .bar-track { flex: 1; height: 6px; background: #f1f5f9; border-radius: 3px; margin: 0 8px; }
    .bar-fill { height: 100%; border-radius: 3px; }
    .bar-value { width: 28px; font-size: 11px; font-weight: 500; color: #64748b; text-align: right; }
    .empty { color: #94a3b8; font-size: 13px; }
    .horizon-section { display: flex; flex-direction: column; gap: 16px; }
    .horizon-card { background: linear-gradient(135deg, #faf5ff 0%, #f5f3ff 100%); border: 1px solid #c4b5fd; border-radius: 8px; padding: 16px; }
    .horizon-title { font-weight: 600; font-size: 15px; color: #5b21b6; margin-bottom: 8px; }
    .horizon-possible { font-size: 14px; color: #334155; margin-bottom: 10px; line-height: 1.5; }
    .horizon-tip { font-size: 13px; color: #6b21a8; background: rgba(255,255,255,0.6); padding: 8px 12px; border-radius: 4px; }
    .feedback-header { margin-top: 48px; color: #64748b; font-size: 16px; }
    .feedback-intro { font-size: 13px; color: #94a3b8; margin-bottom: 16px; }
    .feedback-section { margin-top: 16px; }
    .feedback-section h3 { font-size: 14px; font-weight: 600; color: #475569; margin-bottom: 12px; }
    .feedback-card { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; margin-bottom: 12px; }
    .feedback-card.team-card { background: #eff6ff; border-color: #bfdbfe; }
    .feedback-card.model-card { background: #faf5ff; border-color: #e9d5ff; }
    .feedback-title { font-weight: 600; font-size: 14px; color: #0f172a; margin-bottom: 6px; }
    .feedback-detail { font-size: 13px; color: #475569; line-height: 1.5; }
    .feedback-evidence { font-size: 12px; color: #64748b; margin-top: 8px; }
    .fun-ending { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border: 1px solid #fbbf24; border-radius: 12px; padding: 24px; margin-top: 40px; text-align: center; }
    .fun-headline { font-size: 18px; font-weight: 600; color: #78350f; margin-bottom: 8px; }
    .fun-detail { font-size: 14px; color: #92400e; }
    .collapsible-section { margin-top: 16px; }
    .collapsible-header { display: flex; align-items: center; gap: 8px; cursor: pointer; padding: 12px 0; border-bottom: 1px solid #e2e8f0; }
    .collapsible-header h3 { margin: 0; font-size: 14px; font-weight: 600; color: #475569; }
    .collapsible-arrow { font-size: 12px; color: #94a3b8; transition: transform 0.2s; }
    .collapsible-content { display: none; padding-top: 16px; }
    .collapsible-content.open { display: block; }
    .collapsible-header.open .collapsible-arrow { transform: rotate(90deg); }
    @media (max-width: 640px) { .charts-row { grid-template-columns: 1fr; } .stats-row { justify-content: center; } }
  `,
    B = `
    function toggleCollapsible(header) {
      header.classList.toggle('open');
      const content = header.nextElementSibling;
      content.classList.toggle('open');
    }
    function copyText(btn) {
      const code = btn.previousElementSibling;
      navigator.clipboard.writeText(code.textContent).then(() => {
        btn.textContent = 'Copied!';
        setTimeout(() => { btn.textContent = 'Copy'; }, 2000);
      });
    }
    function copyCmdItem(idx) {
      const checkbox = document.getElementById('cmd-' + idx);
      if (checkbox) {
        const text = checkbox.dataset.text;
        navigator.clipboard.writeText(text).then(() => {
          const btn = checkbox.nextElementSibling.querySelector('.copy-btn');
          if (btn) { btn.textContent = 'Copied!'; setTimeout(() => { btn.textContent = 'Copy'; }, 2000); }
        });
      }
    }
    function copyAllCheckedClaudeMd() {
      const checkboxes = document.querySelectorAll('.cmd-checkbox:checked');
      const texts = [];
      checkboxes.forEach(cb => {
        if (cb.dataset.text) { texts.push(cb.dataset.text); }
      });
      const combined = texts.join('\\n');
      const btn = document.querySelector('.copy-all-btn');
      if (btn) {
        navigator.clipboard.writeText(combined).then(() => {
          btn.textContent = 'Copied ' + texts.length + ' items!';
          btn.classList.add('copied');
          setTimeout(() => { btn.textContent = 'Copy All Checked'; btn.classList.remove('copied'); }, 2000);
        });
      }
    }
    // Timezone selector for time of day chart (data is from our own analytics, not user input)
    const rawHourCounts = ${bt(e.message_hours)};
    function updateHourHistogram(offsetFromPT) {
      const periods = [
        { label: "Morning (6-12)", range: [6,7,8,9,10,11] },
        { label: "Afternoon (12-18)", range: [12,13,14,15,16,17] },
        { label: "Evening (18-24)", range: [18,19,20,21,22,23] },
        { label: "Night (0-6)", range: [0,1,2,3,4,5] }
      ];
      const adjustedCounts = {};
      for (const [hour, count] of Object.entries(rawHourCounts)) {
        const newHour = (parseInt(hour) + offsetFromPT + 24) % 24;
        adjustedCounts[newHour] = (adjustedCounts[newHour] || 0) + count;
      }
      const periodCounts = periods.map(p => ({
        label: p.label,
        count: p.range.reduce((sum, h) => sum + (adjustedCounts[h] || 0), 0)
      }));
      const maxCount = Math.max(...periodCounts.map(p => p.count)) || 1;
      const container = document.getElementById('hour-histogram');
      container.textContent = '';
      periodCounts.forEach(p => {
        const row = document.createElement('div');
        row.className = 'bar-row';
        const label = document.createElement('div');
        label.className = 'bar-label';
        label.textContent = p.label;
        const track = document.createElement('div');
        track.className = 'bar-track';
        const fill = document.createElement('div');
        fill.className = 'bar-fill';
        fill.style.width = (p.count / maxCount) * 100 + '%';
        fill.style.background = '#8b5cf6';
        track.appendChild(fill);
        const value = document.createElement('div');
        value.className = 'bar-value';
        value.textContent = p.count;
        row.appendChild(label);
        row.appendChild(track);
        row.appendChild(value);
        container.appendChild(row);
      });
    }
    document.getElementById('timezone-select').addEventListener('change', function() {
      const customInput = document.getElementById('custom-offset');
      if (this.value === 'custom') {
        customInput.style.display = 'inline-block';
        customInput.focus();
      } else {
        customInput.style.display = 'none';
        updateHourHistogram(parseInt(this.value));
      }
    });
    document.getElementById('custom-offset').addEventListener('change', function() {
      const parsed = parseInt(this.value, 10);
      if (isNaN(parsed)) return;
      updateHourHistogram(parsed + 8);
    });
  `;
  return `<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Claude Code Insights</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; background: #f8fafc; color: #334155; line-height: 1.65; padding: 48px 24px; }
    .container { max-width: 800px; margin: 0 auto; }
    h1 { font-size: 32px; font-weight: 700; color: #0f172a; margin-bottom: 8px; }
    h2 { font-size: 20px; font-weight: 600; color: #0f172a; margin-top: 48px; margin-bottom: 16px; }
    .subtitle { color: #64748b; font-size: 15px; margin-bottom: 32px; }
    .nav-toc { display: flex; flex-wrap: wrap; gap: 8px; margin: 24px 0 32px 0; padding: 16px; background: white; border-radius: 8px; border: 1px solid #e2e8f0; }
    .nav-toc a { font-size: 12px; color: #64748b; text-decoration: none; padding: 6px 12px; border-radius: 6px; background: #f1f5f9; transition: all 0.15s; }
    .nav-toc a:hover { background: #e2e8f0; color: #334155; }
    .stats-row { display: flex; gap: 24px; margin-bottom: 40px; padding: 20px 0; border-top: 1px solid #e2e8f0; border-bottom: 1px solid #e2e8f0; flex-wrap: wrap; }
    .stat { text-align: center; }
    .stat-value { font-size: 24px; font-weight: 700; color: #0f172a; }
    .stat-label { font-size: 11px; color: #64748b; text-transform: uppercase; }
    .at-a-glance { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border: 1px solid #f59e0b; border-radius: 12px; padding: 20px 24px; margin-bottom: 32px; }
    .glance-title { font-size: 16px; font-weight: 700; color: #92400e; margin-bottom: 16px; }
    .glance-sections { display: flex; flex-direction: column; gap: 12px; }
    .glance-section { font-size: 14px; color: #78350f; line-height: 1.6; }
    .glance-section strong { color: #92400e; }
    .see-more { color: #b45309; text-decoration: none; font-size: 13px; white-space: nowrap; }
    .see-more:hover { text-decoration: underline; }
    .project-areas { display: flex; flex-direction: column; gap: 12px; margin-bottom: 32px; }
    .project-area { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; }
    .area-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
    .area-name { font-weight: 600; font-size: 15px; color: #0f172a; }
    .area-count { font-size: 12px; color: #64748b; background: #f1f5f9; padding: 2px 8px; border-radius: 4px; }
    .area-desc { font-size: 14px; color: #475569; line-height: 1.5; }
    .narrative { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 20px; margin-bottom: 24px; }
    .narrative p { margin-bottom: 12px; font-size: 14px; color: #475569; line-height: 1.7; }
    .key-insight { background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 12px 16px; margin-top: 12px; font-size: 14px; color: #166534; }
    .section-intro { font-size: 14px; color: #64748b; margin-bottom: 16px; }
    .big-wins { display: flex; flex-direction: column; gap: 12px; margin-bottom: 24px; }
    .big-win { background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 16px; }
    .big-win-title { font-weight: 600; font-size: 15px; color: #166534; margin-bottom: 8px; }
    .big-win-desc { font-size: 14px; color: #15803d; line-height: 1.5; }
    .friction-categories { display: flex; flex-direction: column; gap: 16px; margin-bottom: 24px; }
    .friction-category { background: #fef2f2; border: 1px solid #fca5a5; border-radius: 8px; padding: 16px; }
    .friction-title { font-weight: 600; font-size: 15px; color: #991b1b; margin-bottom: 6px; }
    .friction-desc { font-size: 13px; color: #7f1d1d; margin-bottom: 10px; }
    .friction-examples { margin: 0 0 0 20px; font-size: 13px; color: #334155; }
    .friction-examples li { margin-bottom: 4px; }
    .claude-md-section { background: #eff6ff; border: 1px solid #bfdbfe; border-radius: 8px; padding: 16px; margin-bottom: 20px; }
    .claude-md-section h3 { font-size: 14px; font-weight: 600; color: #1e40af; margin: 0 0 12px 0; }
    .claude-md-actions { margin-bottom: 12px; padding-bottom: 12px; border-bottom: 1px solid #dbeafe; }
    .copy-all-btn { background: #2563eb; color: white; border: none; border-radius: 4px; padding: 6px 12px; font-size: 12px; cursor: pointer; font-weight: 500; transition: all 0.2s; }
    .copy-all-btn:hover { background: #1d4ed8; }
    .copy-all-btn.copied { background: #16a34a; }
    .claude-md-item { display: flex; flex-wrap: wrap; align-items: flex-start; gap: 8px; padding: 10px 0; border-bottom: 1px solid #dbeafe; }
    .claude-md-item:last-child { border-bottom: none; }
    .cmd-checkbox { margin-top: 2px; }
    .cmd-code { background: white; padding: 8px 12px; border-radius: 4px; font-size: 12px; color: #1e40af; border: 1px solid #bfdbfe; font-family: monospace; display: block; white-space: pre-wrap; word-break: break-word; flex: 1; }
    .cmd-why { font-size: 12px; color: #64748b; width: 100%; padding-left: 24px; margin-top: 4px; }
    .features-section, .patterns-section { display: flex; flex-direction: column; gap: 12px; margin: 16px 0; }
    .feature-card { background: #f0fdf4; border: 1px solid #86efac; border-radius: 8px; padding: 16px; }
    .pattern-card { background: #f0f9ff; border: 1px solid #7dd3fc; border-radius: 8px; padding: 16px; }
    .feature-title, .pattern-title { font-weight: 600; font-size: 15px; color: #0f172a; margin-bottom: 6px; }
    .feature-oneliner { font-size: 14px; color: #475569; margin-bottom: 8px; }
    .pattern-summary { font-size: 14px; color: #475569; margin-bottom: 8px; }
    .feature-why, .pattern-detail { font-size: 13px; color: #334155; line-height: 1.5; }
    .feature-examples { margin-top: 12px; }
    .feature-example { padding: 8px 0; border-top: 1px solid #d1fae5; }
    .feature-example:first-child { border-top: none; }
    .example-desc { font-size: 13px; color: #334155; margin-bottom: 6px; }
    .example-code-row { display: flex; align-items: flex-start; gap: 8px; }
    .example-code { flex: 1; background: #f1f5f9; padding: 8px 12px; border-radius: 4px; font-family: monospace; font-size: 12px; color: #334155; overflow-x: auto; white-space: pre-wrap; }
    .copyable-prompt-section { margin-top: 12px; padding-top: 12px; border-top: 1px solid #e2e8f0; }
    .copyable-prompt-row { display: flex; align-items: flex-start; gap: 8px; }
    .copyable-prompt { flex: 1; background: #f8fafc; padding: 10px 12px; border-radius: 4px; font-family: monospace; font-size: 12px; color: #334155; border: 1px solid #e2e8f0; white-space: pre-wrap; line-height: 1.5; }
    .feature-code { background: #f8fafc; padding: 12px; border-radius: 6px; margin-top: 12px; border: 1px solid #e2e8f0; display: flex; align-items: flex-start; gap: 8px; }
    .feature-code code { flex: 1; font-family: monospace; font-size: 12px; color: #334155; white-space: pre-wrap; }
    .pattern-prompt { background: #f8fafc; padding: 12px; border-radius: 6px; margin-top: 12px; border: 1px solid #e2e8f0; }
    .pattern-prompt code { font-family: monospace; font-size: 12px; color: #334155; display: block; white-space: pre-wrap; margin-bottom: 8px; }
    .prompt-label { font-size: 11px; font-weight: 600; text-transform: uppercase; color: #64748b; margin-bottom: 6px; }
    .copy-btn { background: #e2e8f0; border: none; border-radius: 4px; padding: 4px 8px; font-size: 11px; cursor: pointer; color: #475569; flex-shrink: 0; }
    .copy-btn:hover { background: #cbd5e1; }
    .charts-row { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin: 24px 0; }
    .chart-card { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; }
    .chart-title { font-size: 12px; font-weight: 600; color: #64748b; text-transform: uppercase; margin-bottom: 12px; }
    .bar-row { display: flex; align-items: center; margin-bottom: 6px; }
    .bar-label { width: 100px; font-size: 11px; color: #475569; flex-shrink: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .bar-track { flex: 1; height: 6px; background: #f1f5f9; border-radius: 3px; margin: 0 8px; }
    .bar-fill { height: 100%; border-radius: 3px; }
    .bar-value { width: 28px; font-size: 11px; font-weight: 500; color: #64748b; text-align: right; }
    .empty { color: #94a3b8; font-size: 13px; }
    .horizon-section { display: flex; flex-direction: column; gap: 16px; }
    .horizon-card { background: linear-gradient(135deg, #faf5ff 0%, #f5f3ff 100%); border: 1px solid #c4b5fd; border-radius: 8px; padding: 16px; }
    .horizon-title { font-weight: 600; font-size: 15px; color: #5b21b6; margin-bottom: 8px; }
    .horizon-possible { font-size: 14px; color: #334155; margin-bottom: 10px; line-height: 1.5; }
    .horizon-tip { font-size: 13px; color: #6b21a8; background: rgba(255,255,255,0.6); padding: 8px 12px; border-radius: 4px; }
    .feedback-header { margin-top: 48px; color: #64748b; font-size: 16px; }
    .feedback-intro { font-size: 13px; color: #94a3b8; margin-bottom: 16px; }
    .feedback-section { margin-top: 16px; }
    .feedback-section h3 { font-size: 14px; font-weight: 600; color: #475569; margin-bottom: 12px; }
    .feedback-card { background: white; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px; margin-bottom: 12px; }
    .feedback-card.team-card { background: #eff6ff; border-color: #bfdbfe; }
    .feedback-card.model-card { background: #faf5ff; border-color: #e9d5ff; }
    .feedback-title { font-weight: 600; font-size: 14px; color: #0f172a; margin-bottom: 6px; }
    .feedback-detail { font-size: 13px; color: #475569; line-height: 1.5; }
    .feedback-evidence { font-size: 12px; color: #64748b; margin-top: 8px; }
    .fun-ending { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border: 1px solid #fbbf24; border-radius: 12px; padding: 24px; margin-top: 40px; text-align: center; }
    .fun-headline { font-size: 18px; font-weight: 600; color: #78350f; margin-bottom: 8px; }
    .fun-detail { font-size: 14px; color: #92400e; }
    .collapsible-section { margin-top: 16px; }
    .collapsible-header { display: flex; align-items: center; gap: 8px; cursor: pointer; padding: 12px 0; border-bottom: 1px solid #e2e8f0; }
    .collapsible-header h3 { margin: 0; font-size: 14px; font-weight: 600; color: #475569; }
    .collapsible-arrow { font-size: 12px; color: #94a3b8; transition: transform 0.2s; }
    .collapsible-content { display: none; padding-top: 16px; }
    .collapsible-content.open { display: block; }
    .collapsible-header.open .collapsible-arrow { transform: rotate(90deg); }
    @media (max-width: 640px) { .charts-row { grid-template-columns: 1fr; } .stats-row { justify-content: center; } }
  </style>
</head>
<body>
  <div class="container">
    <h1>Claude Code Insights</h1>
    <p class="subtitle">${e.total_messages.toLocaleString()} messages across ${e.total_sessions} sessions${e.total_sessions_scanned && e.total_sessions_scanned > e.total_sessions ? ` (${e.total_sessions_scanned.toLocaleString()} total)` : ""} | ${e.date_range.start} to ${e.date_range.end}</p>

    ${l}

    <nav class="nav-toc">
      <a href="#section-work">What You Work On</a>
      <a href="#section-usage">How You Use CC</a>
      <a href="#section-wins">Impressive Things</a>
      <a href="#section-friction">Where Things Go Wrong</a>
      <a href="#section-features">Features to Try</a>
      <a href="#section-patterns">New Usage Patterns</a>
      <a href="#section-horizon">On the Horizon</a>
      <a href="#section-feedback">Team Feedback</a>
    </nav>

    <div class="stats-row">
      <div class="stat"><div class="stat-value">${e.total_messages.toLocaleString()}</div><div class="stat-label">Messages</div></div>
      <div class="stat"><div class="stat-value">+${e.total_lines_added.toLocaleString()}/-${e.total_lines_removed.toLocaleString()}</div><div class="stat-label">Lines</div></div>
      <div class="stat"><div class="stat-value">${e.total_files_modified}</div><div class="stat-label">Files</div></div>
      <div class="stat"><div class="stat-value">${e.days_active}</div><div class="stat-label">Days</div></div>
      <div class="stat"><div class="stat-value">${e.messages_per_day}</div><div class="stat-label">Msgs/Day</div></div>
    </div>

    ${u}

    <div class="charts-row">
      <div class="chart-card">
        <div class="chart-title">What You Wanted</div>
        ${G(e.goal_categories, "#2563eb")}
      </div>
      <div class="chart-card">
        <div class="chart-title">Top Tools Used</div>
        ${G(e.tool_counts, "#0891b2")}
      </div>
    </div>

    <div class="charts-row">
      <div class="chart-card">
        <div class="chart-title">Languages</div>
        ${G(e.languages, "#10b981")}
      </div>
      <div class="chart-card">
        <div class="chart-title">Session Types</div>
        ${G(e.session_types || {}, "#8b5cf6")}
      </div>
    </div>

    ${p}

    <!-- Response Time Distribution -->
    <div class="chart-card" style="margin: 24px 0;">
      <div class="chart-title">User Response Time Distribution</div>
      ${ft(e.user_response_times)}
      <div style="font-size: 12px; color: #64748b; margin-top: 8px;">
        Median: ${e.median_response_time.toFixed(1)}s &bull; Average: ${e.avg_response_time.toFixed(1)}s
      </div>
    </div>

    <!-- Multi-clauding Section (matching Python reference) -->
    <div class="chart-card" style="margin: 24px 0;">
      <div class="chart-title">Multi-Clauding (Parallel Sessions)</div>
      ${
        e.multi_clauding.overlap_events === 0
          ? `
        <p style="font-size: 14px; color: #64748b; padding: 8px 0;">
          No parallel session usage detected. You typically work with one Claude Code session at a time.
        </p>
      `
          : `
        <div style="display: flex; gap: 24px; margin: 12px 0;">
          <div style="text-align: center;">
            <div style="font-size: 24px; font-weight: 700; color: #7c3aed;">${e.multi_clauding.overlap_events}</div>
            <div style="font-size: 11px; color: #64748b; text-transform: uppercase;">Overlap Events</div>
          </div>
          <div style="text-align: center;">
            <div style="font-size: 24px; font-weight: 700; color: #7c3aed;">${e.multi_clauding.sessions_involved}</div>
            <div style="font-size: 11px; color: #64748b; text-transform: uppercase;">Sessions Involved</div>
          </div>
          <div style="text-align: center;">
            <div style="font-size: 24px; font-weight: 700; color: #7c3aed;">${e.total_messages > 0 ? Math.round((100 * e.multi_clauding.user_messages_during) / e.total_messages) : 0}%</div>
            <div style="font-size: 11px; color: #64748b; text-transform: uppercase;">Of Messages</div>
          </div>
        </div>
        <p style="font-size: 13px; color: #475569; margin-top: 12px;">
          You run multiple Claude Code sessions simultaneously. Multi-clauding is detected when sessions
          overlap in time, suggesting parallel workflows.
        </p>
      `
      }
    </div>

    <!-- Time of Day & Tool Errors -->
    <div class="charts-row">
      <div class="chart-card">
        <div class="chart-title" style="display: flex; align-items: center; gap: 12px;">
          User Messages by Time of Day
          <select id="timezone-select" style="font-size: 12px; padding: 4px 8px; border-radius: 4px; border: 1px solid #e2e8f0;">
            <option value="0">PT (UTC-8)</option>
            <option value="3">ET (UTC-5)</option>
            <option value="8">London (UTC)</option>
            <option value="9">CET (UTC+1)</option>
            <option value="17">Tokyo (UTC+9)</option>
            <option value="custom">Custom offset...</option>
          </select>
          <input type="number" id="custom-offset" placeholder="UTC offset" style="display: none; width: 80px; font-size: 12px; padding: 4px; border-radius: 4px; border: 1px solid #e2e8f0;">
        </div>
        ${ht(e.message_hours)}
      </div>
      <div class="chart-card">
        <div class="chart-title">Tool Errors Encountered</div>
        ${Object.keys(e.tool_error_categories).length > 0 ? G(e.tool_error_categories, "#dc2626") : '<p class="empty">No tool errors</p>'}
      </div>
    </div>

    ${f}

    <div class="charts-row">
      <div class="chart-card">
        <div class="chart-title">What Helped Most (Claude's Capabilities)</div>
        ${G(e.success, "#16a34a")}
      </div>
      <div class="chart-card">
        <div class="chart-title">Outcomes</div>
        ${G(e.outcomes, "#8b5cf6", 6, mt)}
      </div>
    </div>

    ${R}

    <div class="charts-row">
      <div class="chart-card">
        <div class="chart-title">Primary Friction Types</div>
        ${G(e.friction, "#dc2626")}
      </div>
      <div class="chart-card">
        <div class="chart-title">Inferred Satisfaction (model-estimated)</div>
        ${G(e.satisfaction, "#eab308", 6, pt)}
      </div>
    </div>

    ${j}

    ${L}

    ${U}

    ${y}
  </div>
  <script>${B}</script>
</body>
</html>`;
}
function ps(e, s, o, r) {
  let l =
      typeof {
        ISSUES_EXPLAINER: "report the issue at https://github.com/anthropics/claude-code/issues",
        PACKAGE_URL: "@anthropic-ai/claude-code",
        README_URL: "https://code.claude.com/docs/en/overview",
        VERSION: "2.1.259",
        FEEDBACK_CHANNEL: "https://github.com/anthropics/claude-code/issues",
        BUILD_TIME: "2026-09-02T18:43:49Z",
        GIT_SHA: "9b549c8d1c72e407ea9d3af3b9d5e50da794ec4d",
        HOOKS_WORKER_URL: "/$bunfs/root/src/plugins/functionHooks/hooks-worker/hooks-worker.js",
        DD_SOURCEMAP_GROUP: "default",
      } < "u"
        ? {
            ISSUES_EXPLAINER:
              "report the issue at https://github.com/anthropics/claude-code/issues",
            PACKAGE_URL: "@anthropic-ai/claude-code",
            README_URL: "https://code.claude.com/docs/en/overview",
            VERSION: "2.1.259",
            FEEDBACK_CHANNEL: "https://github.com/anthropics/claude-code/issues",
            BUILD_TIME: "2026-09-02T18:43:49Z",
            GIT_SHA: "9b549c8d1c72e407ea9d3af3b9d5e50da794ec4d",
            HOOKS_WORKER_URL: "/$bunfs/root/src/plugins/functionHooks/hooks-worker/hooks-worker.js",
            DD_SOURCEMAP_GROUP: "default",
          }.VERSION
        : "unknown",
    d = r?.hosts.filter((n) => n.sessionCount > 0).map((n) => n.name),
    u = { total: o.size, goal_categories: {}, outcomes: {}, satisfaction: {}, friction: {} };
  for (let n of o.values()) {
    for (let [p, a] of ee(n.goal_categories))
      if (a > 0) u.goal_categories[p] = (u.goal_categories[p] || 0) + a;
    u.outcomes[n.outcome] = (u.outcomes[n.outcome] || 0) + 1;
    for (let [p, a] of ee(n.user_satisfaction_counts))
      if (a > 0) u.satisfaction[p] = (u.satisfaction[p] || 0) + a;
    for (let [p, a] of ee(n.friction_counts)) if (a > 0) u.friction[p] = (u.friction[p] || 0) + a;
  }
  return {
    metadata: {
      username: process.env.SAFEUSER || process.env.USER || "unknown",
      generated_at: new Date().toISOString(),
      claude_code_version: l,
      date_range: e.date_range,
      session_count: e.total_sessions,
      ...(d && d.length > 0 && { remote_hosts_collected: d }),
    },
    aggregated_data: e,
    insights: s,
    facets_summary: u,
  };
}
async function xt(e) {
  if (e) {
    let d = [];
    switch (
      (
        await Ro(
          (f) => e.listEntries({ namespace: "transcript" }, f ? { cursor: f } : void 0),
          (f) => {
            for (let x of f)
              if (
                x.kind === "scope" &&
                x.scope.namespace === "transcript" &&
                x.scope.projectKey !== void 0
              )
                d.push(x.scope.projectKey);
          },
        )
      ).status
    ) {
      case "done":
        break;
      case "error":
        return [];
      case "capped":
        t(
          `insights: project listing truncated at ${tc} pages; scanning the ${d.length} projects seen`,
          { level: "warn" },
        );
        break;
    }
    let n = [],
      p = { pagesLeft: tc },
      a = 0;
    for (let f = 0; f < d.length; f++) {
      let x = d[f];
      switch (
        (
          await Ro(
            (v) =>
              e.listEntries({ namespace: "transcript", projectKey: x }, v ? { cursor: v } : void 0),
            (v) => {
              for (let j of v) {
                if (j.kind !== "key" || j.key.namespace !== "transcript") continue;
                if (j.key.agentId !== void 0 || j.key.journal === !0 || j.key.projectKey !== x)
                  continue;
                let z = Vn(j.key.sessionId);
                if (!z) continue;
                n.push({
                  sessionId: z,
                  key: ve.transcript(x, z),
                  mtime: j.mtimeMs ?? 0,
                  size: j.size ?? 0,
                });
              }
            },
            { budget: p },
          )
        ).status
      ) {
        case "done":
          break;
        case "error":
          break;
        case "capped":
          if (a === 0)
            t(
              `insights: session listing for project ${x} truncated (the scan's ${tc}-page session-listing budget is spent); keeping the sessions seen`,
              { level: "warn" },
            );
          a++;
          break;
      }
      if (f % 10 === 9) await new Promise((v) => setImmediate(v));
    }
    if (a > 1)
      t(
        `insights: ${a - 1} more ${a === 2 ? "project was" : "projects were"} left without a session listing after the scan's budget ran out`,
        { level: "warn" },
      );
    return (n.sort((f, x) => x.mtime - f.mtime), n);
  }
  let s = Nl(),
    o;
  try {
    o = await De(s, { withFileTypes: !0 });
  } catch {
    return [];
  }
  let r = o.filter((d) => d.isDirectory()).map((d) => q(s, d.name)),
    l = [];
  for (let d = 0; d < r.length; d++) {
    let u = await ROe(r[d]);
    for (let [n, p] of u) l.push({ sessionId: n, path: p.path, mtime: p.mtime, size: p.size });
    if (d % 10 === 9) await new Promise((n) => setImmediate(n));
  }
  return (l.sort((d, u) => u.mtime - d.mtime), l);
}
async function wt(e) {
  let s,
    o = await xt(e?.storageV5),
    r = o.length,
    l = 200,
    d = [],
    u = [],
    n = 0,
    p = 0;
  for (let _ of Ce(o, ke)) {
    let I = e?.storageV5,
      N = I
        ? await ze(
            _.map((w) => w.sessionId),
            I,
          )
        : await Promise.all(_.map((w) => it(w.sessionId))),
      V = _.map((w, E) => ({ sessionInfo: w, cached: N[E] ?? null }));
    for (let { sessionInfo: w, cached: E } of V)
      if (E && (E.transcript_mtime === void 0 || E.transcript_mtime >= w.mtime)) d.push(E);
      else if (!E) {
        if (n < l) (u.push({ sessionInfo: w, cached: E }), n++);
      } else if (p < l) (u.push({ sessionInfo: w, cached: E }), p++);
      else d.push(E);
  }
  let a = new Map(),
    f = (_) => {
      for (let I of _.messages.slice(0, 5))
        if (I.type === "user" && I.message) {
          let N = I.message.content;
          if (typeof N === "string") {
            if (N.includes("RESPOND WITH ONLY A VALID JSON OBJECT") || N.includes("record_facets"))
              return !0;
          }
        }
      return !1;
    },
    x = 10;
  for (let _ = 0; _ < u.length; _ += x) {
    let I = u.slice(_, _ + x),
      N = await Promise.all(
        I.map(async ({ sessionInfo: w, cached: E }) => {
          try {
            return {
              sessionInfo: w,
              cached: E,
              logs:
                e?.storageV5 && w.key
                  ? await J6e(`${w.sessionId}.jsonl`, void 0, { backend: e.storageV5, key: w.key })
                  : await J6e(w.path ?? ""),
            };
          } catch {
            return { sessionInfo: w, cached: E, logs: [] };
          }
        }),
      ),
      V = new Map();
    for (let { sessionInfo: w, cached: E, logs: ie } of N) {
      let ye = !1;
      for (let te of ie) {
        if (f(te) || Number.isNaN(te.created.getTime()) || Number.isNaN(te.modified.getTime()))
          continue;
        let X = me(te);
        if (((X.transcript_mtime = w.mtime), d.push(X), (ye = !0), pe(X, V.get(X.session_id))))
          V.set(X.session_id, X);
        a.set(X.session_id, te);
      }
      if (!ye && E) d.push(E);
    }
    await Promise.all([...V.values()].map((w) => rt(w, e?.storageV5)));
  }
  let R = new Map();
  for (let _ of d) if (pe(_, R.get(_.session_id))) R.set(_.session_id, _);
  let v = new Set(R.keys());
  d = [...R.values()];
  for (let _ of a.keys()) if (!v.has(_)) a.delete(_);
  d.sort((_, I) => I.start_time.localeCompare(_.start_time));
  let j = (_) => {
      if (_.user_message_count < 2) return !1;
      if (_.duration_minutes < 1) return !1;
      return !0;
    },
    z = d.filter(j),
    L = new Map(),
    F = [],
    W = 50,
    y = e?.storageV5,
    P = z.map((_) => _.session_id),
    U = y
      ? (await Promise.all(Ce(P, ke).map((_) => Re(_, y)))).flat()
      : await Promise.all(P.map((_) => ot(_))),
    J = z.map((_, I) => ({ sessionId: _.session_id, cached: U[I] ?? null }));
  for (let { sessionId: _, cached: I } of J)
    if (I) L.set(_, I);
    else {
      let N = a.get(_);
      if (N && F.length < W) F.push({ log: N, sessionId: _ });
    }
  let D = 50;
  for (let _ = 0; _ < F.length; _ += D) {
    let I = F.slice(_, _ + D),
      N = await Promise.all(
        I.map(async ({ log: w, sessionId: E }) => {
          let ie = await at(w, E, e?.credentials);
          return { sessionId: E, newFacets: ie };
        }),
      ),
      V = [];
    for (let { sessionId: w, newFacets: E } of N) if (E) (L.set(w, E), V.push(E));
    await Promise.all(V.map((w) => nt(w, e?.storageV5)));
  }
  let B = (_) => {
      let I = L.get(_);
      if (!I) return !1;
      let N = I.goal_categories,
        V = kt(N).filter((w) => (N[w] ?? 0) > 0);
      return V.length === 1 && V[0] === "warmup_minimal";
    },
    g = z.filter((_) => !B(_.session_id)),
    k = new Map();
  for (let [_, I] of L) if (!B(_)) k.set(_, I);
  let S = lt(g, k);
  S.total_sessions_scanned = r;
  let Q = await ut(S, L, e?.credentials),
    ne = vt(S, Q),
    H = new Date(),
    K = (_) => String(_).padStart(2, "0"),
    Z = `${H.getFullYear()}-${K(H.getMonth() + 1)}-${K(H.getDate())}-${K(H.getHours())}${K(H.getMinutes())}${K(H.getSeconds())}`,
    Oe = await Je(ne, Z, e?.storageV5);
  return { insights: Q, htmlPath: Oe, data: S, remoteStats: s, facets: k };
}
function ee(e) {
  return e ? Object.entries(e) : [];
}
function kt(e) {
  return e ? Object.keys(e) : [];
}
var Ct = {
  type: "prompt",
  name: "insights",
  description: "Generate a report analyzing your Claude Code sessions",
  contentLength: 0,
  progressMessage: "analyzing your sessions",
  source: "builtin",
  async getPromptForCommand(e, s) {
    if (s.options?.isSkillPreload)
      return [
        {
          type: "text",
          text: "The /insights report is generated only when the command is invoked directly.",
        },
      ];
    let o = !1,
      r = [],
      l = !1,
      {
        insights: d,
        htmlPath: u,
        data: n,
        remoteStats: p,
      } = await wt({ collectRemote: o, storageV5: s.storageV5, credentials: s.credentials }),
      a = `file://${u}`,
      x = [
        n.total_sessions_scanned && n.total_sessions_scanned > n.total_sessions
          ? `${n.total_sessions_scanned.toLocaleString()} sessions total \xB7 ${n.total_sessions} analyzed`
          : `${n.total_sessions} sessions`,
        `${n.total_messages.toLocaleString()} messages`,
        `${Math.round(n.total_duration_hours)}h`,
        `${n.git_commits} commits`,
      ].join(" \xB7 "),
      R = "",
      v = d.at_a_glance,
      j = v
        ? `## At a Glance

${v.whats_working ? `**What's working:** ${v.whats_working} See _Impressive Things You Did_.` : ""}

${v.whats_hindering ? `**What's hindering you:** ${v.whats_hindering} See _Where Things Go Wrong_.` : ""}

${v.quick_wins ? `**Quick wins to try:** ${v.quick_wins} See _Features to Try_.` : ""}

${v.ambitious_workflows ? `**Ambitious workflows:** ${v.ambitious_workflows} See _On the Horizon_.` : ""}`
        : "_No insights generated_",
      z = `# Claude Code Insights

${x}
${n.date_range.start} to ${n.date_range.end}
${R}
`;
    return [
      {
        type: "text",
        text: St({
          insightsJson: b(d, null, 2),
          reportUrl: a,
          htmlPath: u,
          facetsDir: ae(),
          header: z,
          summaryText: j,
        }),
      },
    ];
  },
};
function St({
  insightsJson: e,
  reportUrl: s,
  htmlPath: o,
  facetsDir: r,
  header: l,
  summaryText: d,
}) {
  return `The user just ran /insights to generate a usage report analyzing their Claude Code sessions.

Here is the full insights data:
${e}

Report URL: ${s}
HTML file: ${o}
Facets directory: ${r}

At-a-glance summary (for your context only \u2014 the user has not seen any output yet):
${l}${d}

Respond with exactly the following, and nothing else. Do not add, omit, or reword any line:

Your shareable insights report is ready:
${s}

Want to dig into any section or try one of the suggestions?`;
}
function _e(e) {
  if (!e || typeof e !== "object") return !1;
  let s = e;
  return (
    typeof s.underlying_goal === "string" &&
    typeof s.outcome === "string" &&
    typeof s.brief_summary === "string" &&
    s.goal_categories !== null &&
    typeof s.goal_categories === "object" &&
    s.user_satisfaction_counts !== null &&
    typeof s.user_satisfaction_counts === "object" &&
    s.friction_counts !== null &&
    typeof s.friction_counts === "object"
  );
}
var gs = Ct;
export {
  lt as aggregateData,
  ps as buildExportData,
  St as buildInsightsResponsePrompt,
  us as deduplicateSessionBranches,
  gs as default,
  ct as detectMultiClauding,
  Qe as extractToolStats,
  wt as generateUsageReport,
  ot as loadCachedFacets,
  it as loadCachedSessionMeta,
  je as normalizeSessionMeta,
  nt as saveFacets,
  rt as saveSessionMeta,
  Je as saveUsageReport,
  tt as summarizeTranscriptChunk,
};
