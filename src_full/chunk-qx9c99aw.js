// Claude Code is a Beta product per Anthropic's Commercial Terms of Service.
// By using Claude Code, you agree that all code acceptance or rejection decisions you make,
// and the associated conversations in context, constitute Feedback under Anthropic's Commercial Terms,
// and may be used to improve Anthropic's products, including training models.
// You are responsible for reviewing any code suggestions before use.

// (c) Anthropic PBC. All rights reserved. Use is subject to the Legal Agreements outlined here: https://code.claude.com/docs/en/legal-and-compliance.

// Version: 2.1.259
import "./chunk-jdw11prg.js";
import { re } from "./chunk-1mrhsd7s.js";
import "./chunk-kn2qhfka.js";
import { Jt } from "./chunk-m7w5m1w6.js";
import "./chunk-wxd1scze.js";
import "./chunk-b3pxzdmn.js";
import { tt, C, l, ji } from "./chunk-058caznt.js";
import "./chunk-97tbrkcc.js";
import { V, z, FZ } from "./chunk-x1rrg5j2.js";
import { M } from "./chunk-56nvyfje.js";
import { b, Y } from "./chunk-fzpv8ev5.js";
import "./chunk-xmrr4sh8.js";
import { le } from "./chunk-ras23w04.js";
import { m } from "./chunk-55w4bsdv.js";
import "./chunk-9fysw8jv.js";
import { a } from "./chunk-m92n5xra.js";
import "./chunk-6rkpsn9e.js";
import "./chunk-bx79h7g8.js";
import "./chunk-ck0tqv1m.js";
import "./chunk-8mbwgjdd.js";
import "./chunk-9f9fskgc.js";
import "./chunk-spz20jb6.js";
import { Dn, bt, Nc, Jl, tn, hm, oa, d3t } from "./chunk-x722nt0q.js";
import "./chunk-mzmfq60a.js";
import "./chunk-616tsvrd.js";
import "./chunk-5dw4kvcq.js";
import "./chunk-ye42pw2j.js";
import "./chunk-vdqz95a3.js";
import "./chunk-n8g979s0.js";
import "./chunk-mxy52vze.js";
import "./chunk-zk8esmth.js";
import "./chunk-k1wkanbv.js";
import "./chunk-7y5wjz4e.js";
import "./chunk-gxyczd8c.js";
import "./chunk-h96shwz8.js";
import "./chunk-ptdm1fhw.js";
import "./chunk-gzwhm5vd.js";
import "./chunk-nwzn6gxv.js";
import "./chunk-enjww0fp.js";
import "./chunk-0dkpd8qq.js";
import "./chunk-67nd7etf.js";
import "./chunk-7xxnrgeg.js";
import "./chunk-rahwxqh8.js";
import "./chunk-m0a16ehy.js";
import "./chunk-rjxafr3h.js";
import "./chunk-1v541dwj.js";
import "./chunk-21dppk21.js";
import "./chunk-edmcaynh.js";
import "./chunk-77152aqa.js";
import { KA } from "./chunk-xfn8hpdj.js";
import "./chunk-5t2g7ar8.js";
import "./chunk-0xd0k64r.js";
import "./chunk-8trhjkwe.js";
import "./chunk-4z2eqcrq.js";
import "./chunk-a0qeq8pm.js";
import "./chunk-9qgz04yg.js";
import "./chunk-vv5g97a8.js";
import "./chunk-b5ax9mbm.js";
import "./chunk-35w62chd.js";
import "./chunk-2yqsfgga.js";
import "./chunk-trntcdrz.js";
import "./chunk-nmde69vm.js";
import "./chunk-7r03n5n9.js";
import "./chunk-9pd12rac.js";
import "./chunk-a7a5sap3.js";
import "./chunk-3r19kwqx.js";
import "./chunk-55t63zqr.js";
import "./chunk-j2rxdvy0.js";
import "./chunk-33da912m.js";
import "./chunk-x67fwt53.js";
import "./chunk-pwdby7t2.js";
import "./chunk-ta3hhm0z.js";
import { kt } from "./chunk-r8hc3n2z.js";
import "./chunk-1qpkzqbm.js";
import "./chunk-hnqd864x.js";
import "./chunk-hrhk4q9h.js";
import { fe } from "./chunk-gbnf6vz0.js";
import "./chunk-0h1h2vpe.js";
import { J8, M2, khn, Uw } from "./chunk-qpwbvc04.js";
import "./chunk-zwtg7j89.js";
import "./chunk-qyqph83r.js";
import "./chunk-nt36bgfs.js";
import "./chunk-6q0mr3fq.js";
import "./chunk-1k9bsfa7.js";
import "./chunk-t387wqyr.js";
import "./chunk-cezzeybd.js";
import "./chunk-dtz7ymrp.js";
import "./chunk-7bh7nxpg.js";
import "./chunk-8nj3fpx9.js";
import "./chunk-wkxp81p6.js";
import "./chunk-npdpr5xp.js";
import "./chunk-f1ybk250.js";
import "./chunk-zj5hwn6k.js";
import "./chunk-xz3v8y7y.js";
import "./chunk-jqjjqb8p.js";
import "./chunk-jtegkk17.js";
import "./chunk-6dcxrmbt.js";
import "./chunk-jkrywj2s.js";
import "./chunk-dz1fj2ka.js";
import "./chunk-zw75zfcp.js";
import "./chunk-svmkhj92.js";
import "./chunk-dybqhddz.js";
import "./chunk-31rmjnrq.js";
import "./chunk-q0fs2qb2.js";
import "./chunk-a33zz456.js";
import "./chunk-esn2chqn.js";
import "./chunk-1fhzzx7h.js";
import "./chunk-321z9ran.js";
import "./chunk-bbpye6w7.js";
import "./chunk-vvzgt3y1.js";
import "./chunk-5rcp9861.js";
import "./chunk-40xnyyhy.js";
import "./chunk-w75z743m.js";
import "./chunk-71edvt4b.js";
import "./chunk-bzm4f2ge.js";
import "./chunk-mrsbyrhq.js";
import "./chunk-30q5ynkm.js";
import "./chunk-b4cswg8c.js";
import "./chunk-5gp3752w.js";
import "./chunk-sm4f0pvs.js";
import "./chunk-1q5tty9h.js";
import "./chunk-ksctmzwy.js";
import "./chunk-tkq0r7ym.js";
import "./chunk-c5577t1e.js";
import "./chunk-eza32dw3.js";
import "./chunk-qjj8swk2.js";
import "./chunk-y91b9t7k.js";
import "./chunk-a3w4apvy.js";
import "./chunk-31k5d81d.js";
import "./chunk-s98sa0gn.js";
import "./chunk-yz9ewb10.js";
import "./chunk-meget4pa.js";
import "./chunk-b9h3hrm9.js";
import "./chunk-jrks771b.js";
import "./chunk-nh5b9j9c.js";
import "./chunk-pgmqkm8j.js";
import "./chunk-b3pk8jsy.js";
import "./chunk-bqzk1zdc.js";
import "./chunk-9fjy6xex.js";
import "./chunk-rnzbwgja.js";
import "./chunk-yj7edqzj.js";
import "./chunk-bzxfr890.js";
import "./chunk-0f1244y5.js";
import "./chunk-t8rh9pbw.js";
import "./chunk-wyqz2xfr.js";
import "./chunk-7b08b87y.js";
import "./chunk-11mcqc6q.js";
import "./chunk-vpt6j0hj.js";
import "./chunk-0697cd2s.js";
import "./chunk-1kx3xrw0.js";
import "./chunk-8gvda1c5.js";
import "./chunk-752tnqtv.js";
import "./chunk-6nznq4h0.js";
import "./chunk-075s54dh.js";
import "./chunk-m97fmp5a.js";
import "./chunk-cj0gpwwk.js";
import "./chunk-qpqhpjnf.js";
import "./chunk-9tnnrncs.js";
import "./chunk-xxq66zap.js";
import "./chunk-67zd0pq0.js";
import "./chunk-13k2svs7.js";
import "./chunk-kxcfgh7d.js";
import "./chunk-abnm2sa9.js";
import "./chunk-m1g84jsb.js";
import "./chunk-jea0jsvq.js";
import "./chunk-f7czbtwe.js";
import "./chunk-grn2cpxb.js";
import "./chunk-s2qv9t07.js";
import "./chunk-rzhk9vzz.js";
import "./chunk-h3nnndyy.js";
import "./chunk-xdnjqx4p.js";
import "./chunk-68asg28d.js";
import "./chunk-at3x5y7v.js";
import "./chunk-wfap5c8g.js";
import "./chunk-mq6t5gsy.js";
import {
  wv,
  HSe,
  LKe,
  xsn,
  Cte,
  Ite,
  Rte,
  ddt,
  Xce,
  K9,
  wSe,
  PWn,
  DWn,
  PKe,
  $Wn,
  MWn,
} from "./chunk-qk9y8twk.js";
import "./chunk-5j20p3d9.js";
import { RKe, kte, wWn, Lz, EWn, HDe, xKe, Tte, idt, vWn, kWn } from "./chunk-dmhmszft.js";
import { bSe, HWn } from "./chunk-m5cen9av.js";
import "./chunk-1bt18d98.js";
import "./chunk-cab72szz.js";
import "./chunk-h2dtywn6.js";
import "./chunk-gmdrem8f.js";
import { czn } from "./chunk-hp4jha7k.js";
import "./chunk-4amf009w.js";
import "./chunk-zbjsms1x.js";
import "./chunk-gt0sz7qx.js";
import "./chunk-8cb3kpz7.js";
import "./chunk-kvmgr8kf.js";
import "./chunk-gyc9r9jx.js";
import "./chunk-fpm5782j.js";
import "./chunk-g41jh2m0.js";
import "./chunk-mz0qwadn.js";
import "./chunk-eft7b88m.js";
import "./chunk-nzscmy2v.js";
import "./chunk-e4vwkrtj.js";
import "./chunk-6dth79yw.js";
import "./chunk-gj0499cq.js";
import "./chunk-szzsnrz5.js";
import "./chunk-pq20xdar.js";
import "./chunk-h9mewq95.js";
import "./chunk-a3vshzn8.js";
import "./chunk-3e1zwnk7.js";
import "./chunk-7bfx680p.js";
import "./chunk-4796xb9w.js";
import "./chunk-31e4aq9f.js";
import "./chunk-90gearh2.js";
import "./chunk-wtntbnrk.js";
import "./chunk-mh620fe5.js";
import "./chunk-h2yhfh1h.js";
import "./chunk-y0c7xssj.js";
import "./chunk-7nw1s99d.js";
import "./chunk-f6ht09n5.js";
import "./chunk-vckrj367.js";
import "./chunk-zc7jwbz1.js";
import "./chunk-50etkfry.js";
import { i, O, de, T, c, nt, ft, ge } from "./chunk-84vc68b7.js";
import "./chunk-c6eb44np.js";
import "./chunk-nc8ww32a.js";
import "./chunk-my1n9ey3.js";
import "./chunk-v10h0yg2.js";
import { Mg } from "./chunk-qjp61mp4.js";
import "./chunk-5m6nm8br.js";
function x(e) {
  return Buffer.byteLength(e, "utf8");
}
var q = {
    list_design_systems: { readOnly: !0, destructive: !1 },
    get_claude_design_prompt: { readOnly: !0, destructive: !1 },
    list_projects: { readOnly: !0, destructive: !1 },
    get_project: { readOnly: !0, destructive: !1 },
    list_files: { readOnly: !0, destructive: !1 },
    read_file: { readOnly: !0, destructive: !1 },
    get_conversation: { readOnly: !0, destructive: !1 },
    list_members: { readOnly: !0, destructive: !1 },
    render_preview: { readOnly: !1, destructive: !1 },
    create_project: { readOnly: !1, destructive: !1 },
    put_conversation: { readOnly: !1, destructive: !1 },
    finalize_plan: { readOnly: !1, destructive: !1 },
    write_files: { readOnly: !1, destructive: !1 },
    copy_files: { readOnly: !1, destructive: !1 },
    create_support_js: { readOnly: !1, destructive: !1 },
    add_member: { readOnly: !1, destructive: !1 },
    delete_files: { readOnly: !1, destructive: !0 },
    remove_member: { readOnly: !1, destructive: !0 },
    update_member_role: { readOnly: !1, destructive: !0 },
    update_sharing: { readOnly: !1, destructive: !0 },
  },
  G = {
    create_project: { top: new Set(["name", "design_system_id"]) },
    put_conversation: {
      top: new Set(["project_id", "title", "messages"]),
      arrays: {
        messages: { fields: new Set(["role", "content"]), required: new Set(["role", "content"]) },
      },
    },
    finalize_plan: {
      top: new Set(["project_id", "writes", "deletes", "scope"]),
      arrays: { writes: "string", deletes: "string" },
      scalarEnums: { scope: new Set(["paths", "project"]) },
    },
    write_files: {
      top: new Set(["project_id", "plan_token", "files"]),
      arrays: {
        files: {
          fields: new Set(["path", "data", "local_path", "encoding", "if_match"]),
          required: new Set(["path"]),
        },
      },
    },
    delete_files: {
      top: new Set(["project_id", "plan_token", "paths", "files"]),
      arrays: {
        paths: "string",
        files: { fields: new Set(["path", "if_match"]), required: new Set(["path"]) },
      },
    },
    copy_files: {
      top: new Set(["project_id", "plan_token", "files"]),
      arrays: {
        files: {
          fields: new Set(["src", "dest", "src_project_id", "if_match"]),
          required: new Set(["src", "dest"]),
        },
      },
    },
    render_preview: {
      top: new Set(["project_id", "path", "render", "validators"]),
      arrays: { validators: "string" },
    },
    create_support_js: { top: new Set(["project_id", "plan_token", "path", "if_match"]) },
    add_member: {
      top: new Set(["project_id", "account_uuid", "email", "role"]),
      scalarEnums: {
        role: { accepts: new Set(["viewer", "commenter", "editor"]), serverNormalizes: !0 },
      },
    },
    update_member_role: {
      top: new Set(["project_id", "account_uuid", "role"]),
      scalarEnums: {
        role: { accepts: new Set(["viewer", "commenter", "editor"]), serverNormalizes: !0 },
      },
    },
    remove_member: { top: new Set(["project_id", "account_uuid"]) },
    update_sharing: {
      top: new Set(["project_id", "scope", "link_permission"]),
      scalarEnums: {
        scope: { accepts: new Set(["invited", "org"]), serverNormalizes: !0 },
        link_permission: { accepts: new Set(["view", "comment", "edit"]), serverNormalizes: !0 },
      },
    },
  },
  W = {
    list: { top: new Set(["full"]) },
    list_design_systems: { top: new Set() },
    get_claude_design_prompt: { top: new Set(["design_system_id", "project_id"]) },
    list_projects: { top: new Set() },
    get_project: { top: new Set(["project_id"]) },
    list_files: { top: new Set(["project_id", "path"]) },
    read_file: { top: new Set(["project_id", "path"]) },
    get_conversation: { top: new Set(["project_id", "chat_id"]) },
    list_members: { top: new Set(["project_id"]) },
  };
function ie(e, t) {
  let n = Object.hasOwn(G, e) ? G[e] : Object.hasOwn(W, e) ? W[e] : void 0;
  if (!n) {
    let o = N(e);
    if (o?.readOnly === !0) return null;
    if (o !== void 0)
      return `ClaudeDesign ${e}: the server reports this operation as write-capable, and this client version can't validate its arguments (it needs a WRITE_OP_SCHEMAS entry). Update Claude Code to use it.`;
    return `ClaudeDesign ${e}: unrecognized operation. If the server added it recently, call {operation: "list"} first \u2014 a read-only operation becomes callable after discovery; a write-tier operation needs a WRITE_OP_SCHEMAS entry in this client.`;
  }
  let r = `ClaudeDesign ${e}`,
    d = (o) => (o.length > 60 ? o.slice(0, 60) + "\u2026" : o);
  if (
    e === "finalize_plan" &&
    t?.scope === "project" &&
    (t.writes !== void 0 || t.deletes !== void 0)
  )
    return `${r}: scope "project" takes no writes/deletes \u2014 a project-scoped plan covers every path in the project.`;
  for (let [o, p] of Object.entries(t ?? {})) {
    if (!n.top.has(o)) {
      let s = [...n.top].find((g) => g.toLowerCase() === o.toLowerCase()),
        u = [...n.top].join(", ");
      return `${r}: unrecognized argument '${d(o)}'${s ? ` (did you mean '${s}'?)` : ""}. ${u ? `Allowed: ${u}.` : "This operation takes no arguments."}`;
    }
    if (!(o in (n.arrays ?? {})) && typeof p === "object" && p !== null)
      return `${r}.${o}: must be a scalar (string/bool/number), not ${Array.isArray(p) ? "an array" : "an object"}.`;
    let _ = o === "plan_token" ? 65536 : 4096;
    if (typeof p === "string" && x(p) > _)
      return `${r}.${o}: too long (${x(p)} bytes; capped at ${_}).`;
    let y = "scalarEnums" in n ? n.scalarEnums?.[o] : void 0;
    if (y !== void 0) {
      let s = "accepts" in y,
        u = s ? y.accepts : y,
        g = s && typeof p === "string" ? p.trim().toLowerCase() : p;
      if (typeof g !== "string" || !u.has(g))
        return `${r}.${o}: must be one of ${[...u].map((w) => `"${w}"`).join(", ")}${s ? " (case-insensitive)" : ""}.`;
      if (s && typeof p === "string" && p.length > g.length + 2)
        return `${r}.${o}: excess whitespace around the value.`;
    }
  }
  for (let [o, p] of Object.entries(n.arrays ?? {})) {
    let _ = t?.[o];
    if (_ === void 0) continue;
    if (!Array.isArray(_)) return `${r}.${o}: must be an array.`;
    for (let y = 0; y < _.length; y++) {
      let s = _[y];
      if (p === "string") {
        if (typeof s !== "string") return `${r}.${o}[${y}]: must be a string.`;
        if (x(s) > 4096) return `${r}.${o}[${y}]: too long (${x(s)} bytes; capped at 4096).`;
      } else {
        if (typeof s !== "object" || s === null || Array.isArray(s))
          return `${r}.${o}[${y}]: must be an object with keys from {${[...p.fields].join(", ")}}.`;
        for (let u of p.required)
          if (typeof s[u] !== "string") return `${r}.${o}[${y}]: missing required field '${u}'.`;
        for (let [u, g] of Object.entries(s)) {
          if (!p.fields.has(u)) {
            let w = [...p.fields].find((k) => k.toLowerCase() === u.toLowerCase());
            return `${r}.${o}[${y}]: unrecognized field '${d(u)}'${w ? ` (did you mean '${w}'?)` : u === "content" ? " (did you mean 'data'? designmcp's write_files reads 'data', not 'content')" : ""}. Allowed: ${[...p.fields].join(", ")}.`;
          }
          if (typeof g !== "string")
            return `${r}.${o}[${y}].${u}: must be a string (got ${Array.isArray(g) ? "array" : typeof g}).`;
          if (u !== "data" && u !== "content" && x(g) > 4096)
            return `${r}.${o}[${y}].${u}: too long (${x(g)} bytes; non-body fields are capped at 4096).`;
        }
      }
    }
  }
  return null;
}
function J(e) {
  return Object.hasOwn(q, e) ? q[e] : void 0;
}
var Ce = m(() =>
  ft({
    name: i(),
    description: i().optional(),
    inputSchema: de().optional(),
    annotations: ft({ readOnlyHint: O().optional(), destructiveHint: O().optional() }).optional(),
  }),
);
class oe {
  byOperation = new Map();
  hintsFor(e) {
    return this.byOperation.get(e);
  }
  knows(e) {
    return this.byOperation.has(e);
  }
  record(e, t) {
    this.byOperation.set(e, t);
  }
  reset() {
    this.byOperation.clear();
  }
}
var Ae = new V(() => new oe());
function te() {
  return Ae.of(z().host);
}
function N(e) {
  return te().hintsFor(e);
}
function ce(e) {
  let t = te();
  t.reset();
  let n = [];
  for (let r of e) {
    let d = Ce().safeParse(r);
    if (!d.success) continue;
    let o = d.data,
      p = Object.hasOwn(q, o.name) ? q[o.name] : void 0,
      _ = o.annotations?.readOnlyHint ?? p?.readOnly ?? !1,
      y = o.annotations?.destructiveHint ?? p?.destructive ?? !0;
    if (p) ((_ &&= p.readOnly), (y ||= p.destructive));
    (t.record(o.name, { readOnly: _, destructive: y }),
      n.push({
        name: o.name,
        description: o.description ?? "",
        inputSchema: o.inputSchema ?? {},
        readOnlyHint: _,
        destructiveHint: y,
      }));
  }
  return n;
}
function ue(e) {
  return !te().knows(e);
}
function B(e, t) {
  if (!t) return e;
  return e.split(t).join("[redacted-oauth-token]");
}
function $e(e) {
  return {
    Authorization: `Bearer ${e}`,
    "Content-Type": "application/json",
    "anthropic-version": "2023-06-01",
    "anthropic-client-platform": Mg(),
  };
}
function E(e) {
  return e < 1024 ? `${e}B` : `${(e / 1024).toFixed(1)}KB`;
}
function L(e) {
  return Buffer.byteLength(e, "utf8");
}
function De(e) {
  switch (e) {
    case "list":
      return "List operations";
    case "list_design_systems":
      return "List design systems";
    case "get_claude_design_prompt":
      return "Get design prompt";
    case "list_projects":
      return "List projects";
    case "get_project":
      return "Read project metadata";
    case "list_files":
      return "List project files";
    case "read_file":
      return "Read file";
    case "get_conversation":
      return "Read conversation";
    case "list_members":
      return "List project members";
    case "create_project":
      return "Create project";
    case "put_conversation":
      return "Write conversation";
    case "finalize_plan":
      return "Finalize plan";
    case "write_files":
      return "Write files";
    case "copy_files":
      return "Copy files";
    case "delete_files":
      return "Delete files";
    case "render_preview":
      return "Render preview";
    case "create_support_js":
      return "Create support.js";
    case "add_member":
      return "Add project member";
    case "update_member_role":
      return "Update member role";
    case "remove_member":
      return "Remove project member";
    case "update_sharing":
      return "Update sharing";
    default:
      return e ?? "";
  }
}
var Re = m(() =>
    nt({
      operation: i()
        .regex(/^[\w.-]{1,64}$/)
        .describe(
          `Claude Design action to perform. Call with "${M2}" first to discover the available operations and their argument schemas.`,
        ),
      arguments: ge(i(), de())
        .default({})
        .describe(
          "Action input object (server-validated). Pass {} for operations that take no input.",
        ),
    }),
  ),
  Ie = m(() => c({ operation: i(), content: T(ge(i(), de())), isError: O().optional() }));
function ne(e) {
  let t = fe(e);
  return (
    !e.options?.isNonInteractiveSession &&
    t.mode !== "bypassPermissions" &&
    !(t.mode === "plan" && t.isBypassPermissionsModeAvailable)
  );
}
var pe =
    "Approving also lets writes and deletes to exactly these paths run without another prompt for up to 15 minutes (file contents are not shown again; anything to any other path will still ask).",
  he = {
    invited: "private: invited members only",
    org: "visible to your whole organization",
    public: "PUBLIC",
  },
  Ee = 60,
  me =
    /[\u0000-\u001F\u007F-\u009F\u00AD\u061C\u180E\u200B\u200E\u200F\u202A-\u202E\u2060-\u2064\u2066-\u2069\u2026\u2800\u3164\uFE00-\uFE0E\uFEFF\uFFF9-\uFFFB\uFFA0]/;
function Oe(e) {
  return (
    /(?:^|\s)[\u200C\u200D\uFE0F]/.test(e) ||
    /[\u200C\u200D](?=\s|$)/.test(e) ||
    /[\u200C\u200D][\u200C\u200D]/.test(e) ||
    /(?:^|[^\p{Emoji}])\uFE0F/u.test(e)
  );
}
var _e = /\uDB40[\uDC20-\uDC7F\uDD00-\uDDEF]|\uD834[\uDD73-\uDD7A]/,
  ye =
    /["\u201C\u201D\u201E\u201F\uFF02\u2033\u2036\u02BA\u02DD\u02EE\u05F4\u3003\u301D-\u301F\u275D\u275E\u2014\u2015]/;
function U(e) {
  if (e.agentContext?.agentType !== "main") return !1;
  try {
    return !Uw("PermissionRequest", e.sessionHooksRegistry, e.agentContext?.agentId ?? "");
  } catch {
    return !1;
  }
}
async function Te(e, t, n, r) {
  try {
    let d = await HDe(r);
    if (d.ok !== !0) return null;
    let o = await Se(e, "get_project", { project_id: t }, d.accessToken, n, null, r);
    if (o.isError === !0) return null;
    for (let p of o.content)
      if (p?.type === "text" && typeof p.text === "string") {
        let _ = Y(p.text),
          { name: y, url: s } = _,
          u = _.sharing?.scope;
        if (typeof y !== "string") return null;
        let g = y.replace(/\s+/g, " ").trim();
        if (g.length === 0 || me.test(g) || _e.test(g) || Oe(g) || g.includes("://")) return null;
        if (ye.test(g)) return null;
        if (typeof u !== "string" || !Object.hasOwn(he, u)) return null;
        if (typeof s !== "string") return null;
        let w;
        try {
          w = new URL(s);
        } catch {
          return null;
        }
        if (
          w.protocol !== "https:" ||
          !w.pathname.endsWith(`/p/${t}`) ||
          w.username !== "" ||
          w.password !== "" ||
          w.search !== "" ||
          w.hash !== "" ||
          s.length > 200 ||
          /\s/.test(s) ||
          ye.test(s) ||
          me.test(s) ||
          _e.test(s) ||
          /[\u200C\u200D\uFE0F]/.test(s)
        )
          return null;
        let k = le(g, Ee);
        return { name: k === g ? g : `${k.trimEnd()}\u2026`, sharingLabel: he[u], url: s };
      }
    return null;
  } catch {
    if (n.aborted) throw new tt();
    return null;
  }
}
var Dt = kt({
    name: J8,
    searchHint: "work with Claude Design (claude.ai/design) projects",
    maxResultSizeChars: 1e5,
    get inputSchema() {
      return Re();
    },
    get outputSchema() {
      return Ie();
    },
    isEnabled: bSe,
    isConcurrencySafe(e) {
      if (e.operation === M2) return !0;
      return (N(e.operation) ?? J(e.operation))?.readOnly === !0;
    },
    isReadOnly(e) {
      if (e.operation === M2) return !0;
      return (N(e.operation) ?? J(e.operation))?.readOnly === !0;
    },
    async description() {
      return khn;
    },
    async prompt() {
      return khn;
    },
    userFacingName(e) {
      let t = De(e?.operation);
      return t ? `Claude Design: ${t}` : "Claude Design";
    },
    toAutoClassifierInput(e) {
      return { operation: e.operation, arguments: e.arguments ?? {} };
    },
    async validateInput(e) {
      let t = ie(e.operation, e.arguments);
      if (t) return { result: !1, message: t, errorCode: 1 };
      if (e.operation === "finalize_plan") {
        let n = e.arguments?.project_id;
        if (typeof n !== "string" || n.length === 0)
          return {
            result: !1,
            message:
              "ClaudeDesign finalize_plan: project_id is required (a plan is always scoped to one project).",
            errorCode: 1,
          };
        if (!/^[A-Za-z0-9._-]+$/.test(n))
          return {
            result: !1,
            message:
              "ClaudeDesign finalize_plan: project_id contains characters outside the server id charset (letters, digits, dot, underscore, dash).",
            errorCode: 1,
          };
        if (n.length > LKe - 2)
          return {
            result: !1,
            message:
              "ClaudeDesign finalize_plan: project_id is longer than any server-issued project id.",
            errorCode: 1,
          };
      }
      return { result: !0 };
    },
    async checkPermissions(e, t) {
      let n = t.toolState.get(wv),
        r = e.operation === M2 ? null : await xKe(n, t.credentials),
        d = {
          ...e,
          __consentBitShown: r,
          __consentAskCanReachUser: ne(t),
          __projectGrantAskShown: void 0,
          __projectGrantServerObserved: void 0,
          __reservedTargetsAskShown: void 0,
          __finalizePlanAskShown: void 0,
        },
        o = r !== null ? kte(r) : null;
      if (e.operation === "finalize_plan" && e.arguments?.scope === "project")
        return {
          behavior: "deny",
          message:
            'ClaudeDesign finalize_plan: scope "project" is no longer supported by this client. Write files directly without plan_token \u2014 the first write to a project asks for a one-time durable approval \u2014 or use finalize_plan with writes/deletes for path-scoped plans and deletes.',
          decisionReason: {
            type: "safetyCheck",
            reason:
              'finalize_plan scope:"project" is superseded by the durable per-project write grant',
            classifierApprovable: !1,
          },
        };
      let p =
          e.operation === M2
            ? { readOnly: !0, destructive: !1 }
            : (N(e.operation) ?? J(e.operation)),
        _ = p?.readOnly !== !0,
        y = _ && p?.destructive !== !1;
      if (
        e.operation === "copy_files" &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      )
        return {
          behavior: "deny",
          message:
            "ClaudeDesign copy_files: copying without a plan_token always requires per-batch approval \u2014 use finalize_plan declaring every destination in writes, then pass the returned plan_token.",
          decisionReason: {
            type: "safetyCheck",
            reason:
              "tokenless copy_files is per-batch-only \u2014 its destinations have no reserved-path or render-integrity gate",
            classifierApprovable: !1,
          },
        };
      if (o) {
        if (
          (e.operation === "write_files" || e.operation === "create_support_js") &&
          !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
        ) {
          let g = PKe(e.operation, e.arguments);
          if (g.outcome !== "pass" || !xsn(g.targets)) {
            let w = g.targets !== void 0 && g.targets.length === 0,
              k = g.outcome === "pass";
            return {
              behavior: "deny",
              message: w
                ? `ClaudeDesign ${e.operation}: this call names no target paths \u2014 list the files to write, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                : k
                  ? `ClaudeDesign ${e.operation}: this batch is too large to display fully for approval \u2014 split it into smaller batches, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                  : `ClaudeDesign ${e.operation}: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason: w
                  ? "a write batch naming no target paths \u2014 malformed call"
                  : k
                    ? "the batch cannot be rendered fully in the approval dialog \u2014 split it or use per-batch plans"
                    : "reserved or unenumerable target paths always require per-batch approval",
                classifierApprovable: !1,
              },
            };
          }
        }
        let s = _ ? `Design ${e.operation} writes to claude.ai/design.` : "",
          u = e.operation === "finalize_plan" ? pe : "";
        return {
          behavior: "ask",
          message: [o, s, u].filter(Boolean).join(" "),
          updatedInput: e.operation === "finalize_plan" ? { ...d, __finalizePlanAskShown: !0 } : d,
          localDisplayOnly: !0,
          decisionReason: {
            type: "safetyCheck",
            reason:
              "design agent consent \u2014 approving records a server-side grant for Claude agents to read and write your design projects",
            classifierApprovable: !1,
          },
        };
      }
      if (!_) return { behavior: "allow", updatedInput: d };
      if (fe(t).mode !== "plan" && U(t) && $Wn(n, e.operation, e.arguments).outcome === "allow")
        return { behavior: "allow", updatedInput: d };
      if (
        (e.operation === "write_files" || e.operation === "create_support_js") &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      ) {
        if (t.options?.isNonInteractiveSession === !0) {
          let u = {
              behavior: "deny",
              message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time interactive project approval, which is not available in non-interactive sessions \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason:
                  "a durable project write grant requires an interactive approval with a server-verified project identity",
                classifierApprovable: !1,
              },
            },
            g = e.arguments?.project_id;
          if (typeof g !== "string" || g.length === 0 || !U(t) || K9(n, g)) return u;
          if (PKe(e.operation, e.arguments).outcome !== "pass") return u;
          if (fe(t).mode === "plan") return u;
          if (ddt(n, g)) return { behavior: "allow", updatedInput: d };
          if ((await idt(n, g, t.credentials)) === "granted" && !K9(n, g)) {
            if ((Rte(n, g), fe(t).mode !== "plan")) return { behavior: "allow", updatedInput: d };
          }
          return u;
        }
        if (!U(t))
          return {
            behavior: "deny",
            message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not available in subagent or PermissionRequest-hook sessions \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
            decisionReason: {
              type: "safetyCheck",
              reason:
                "a durable project write grant requires a context where the approval card reaches the user directly",
              classifierApprovable: !1,
            },
          };
        let s = e.arguments?.project_id;
        if (typeof s === "string" && s.length > 0) {
          if (PWn(n, s))
            return {
              behavior: "deny",
              message: `ClaudeDesign ${e.operation}: this project cannot hold a durable write grant for this account (it may be shared from another organization) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
              decisionReason: {
                type: "safetyCheck",
                reason:
                  "the server refused to mint a durable grant for this project \u2014 per-batch plans only",
                classifierApprovable: !1,
              },
            };
          let u = PKe(e.operation, e.arguments),
            g = fe(t).mode === "plan";
          if (u.outcome === "pass" && g) {
            if (!(
              ddt(n, s) ||
              (!K9(n, s) && (await idt(n, s, t.credentials)) === "granted" && !K9(n, s))
            ))
              return {
                behavior: "deny",
                message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not shown in plan mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason:
                    "a durable project write grant cannot be minted from plan mode \u2014 per-batch plans only",
                  classifierApprovable: !1,
                },
              };
            Rte(n, s);
          } else if (u.outcome === "pass") {
            if (ddt(n, s)) return { behavior: "allow", updatedInput: d };
            let w = await idt(n, s, t.credentials);
            if (w === "granted" && !K9(n, s)) {
              if ((Rte(n, s), fe(t).mode !== "plan")) return { behavior: "allow", updatedInput: d };
            } else if (w === "unavailable")
              return {
                behavior: "deny",
                message: `ClaudeDesign ${e.operation}: could not check for a project write grant (this server may not support durable grants) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason:
                    "the grant state could not be verified \u2014 fail toward the per-batch plan flow",
                  classifierApprovable: !1,
                },
              };
            else {
              let k = () =>
                  fe(t).mode !== "plan"
                    ? null
                    : {
                        behavior: "deny",
                        message: `ClaudeDesign ${e.operation}: writing without a plan_token requires a one-time project approval, which is not shown in plan mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                        decisionReason: {
                          type: "safetyCheck",
                          reason:
                            "a durable project write grant cannot be minted from plan mode \u2014 per-batch plans only",
                          classifierApprovable: !1,
                        },
                      },
                D = k();
              if (D) return D;
              if (!ne(t))
                return {
                  behavior: "deny",
                  message: `ClaudeDesign ${e.operation}: writing without a plan_token to a project without a write grant requires a one-time interactive approval, which cannot be shown in this permission mode \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
                  decisionReason: {
                    type: "safetyCheck",
                    reason:
                      "a durable project write grant requires an interactive approval with a server-verified project identity",
                    classifierApprovable: !1,
                  },
                };
              let h = await Te(n, s, t.abortController.signal, t.credentials);
              if (h === null)
                return {
                  behavior: "deny",
                  message: `ClaudeDesign ${e.operation}: a durable project write grant is only offered when the approval dialog can name its target, and the project identity (name, sharing, URL) could not be verified or rendered faithfully. If this is a fresh connection, read the project first (e.g. get_project \u2014 approve the Claude Design connection if prompted) and retry once; otherwise use finalize_plan with writes/deletes and pass the returned plan_token (the per-batch flow), which is always supported.`,
                  decisionReason: {
                    type: "safetyCheck",
                    reason:
                      "a durable project write grant requires a server-verified project identity in the approval card",
                    classifierApprovable: !1,
                  },
                };
              let S = k();
              if (S) return S;
              return {
                behavior: "ask",
                message: `Approving writes the listed files now, and lets Claude write to ANY file in the project "${h.name}" (${h.sharingLabel}) \u2014 ${h.url} \u2014 without asking again. This approval is remembered for this project until you revoke it in settings at claude.ai/design (future writes and file contents are not shown for approval). Deletes and CLAUDE.md/.claude paths still ask every time.`,
                updatedInput: { ...d, __projectGrantAskShown: s },
                localDisplayOnly: !0,
                serverApprovalWatch: { kind: "design_project_grant", projectId: s },
                decisionReason: {
                  type: "safetyCheck",
                  reason:
                    "first write under a project grant \u2014 approval mints a durable write grant for this project, revocable at claude.ai/design settings",
                  classifierApprovable: !1,
                },
              };
            }
          } else {
            if (!(
              xsn(u.targets) &&
              (ddt(n, s) ||
                (!K9(n, s) && (await idt(n, s, t.credentials)) === "granted" && !K9(n, s)))
            )) {
              let D = u.targets !== void 0 && u.targets.length === 0;
              return {
                behavior: "deny",
                message: D
                  ? `ClaudeDesign ${e.operation}: this call names no target paths \u2014 list the files to write, or use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`
                  : `ClaudeDesign ${e.operation}: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.`,
                decisionReason: {
                  type: "safetyCheck",
                  reason: D
                    ? "a write batch naming no target paths \u2014 malformed call"
                    : "reserved or unenumerable target paths always require per-batch approval",
                  classifierApprovable: !1,
                },
              };
            }
            return (
              Rte(n, s),
              {
                behavior: "ask",
                message: `Design ${e.operation} writes to claude.ai/design.`,
                updatedInput: { ...d, __reservedTargetsAskShown: !0 },
                localDisplayOnly: !0,
                decisionReason: {
                  type: "safetyCheck",
                  reason: y
                    ? "destructive \u2014 model-generated arguments"
                    : "remote write \u2014 model-generated arguments",
                  classifierApprovable: !1,
                },
              }
            );
          }
        }
      }
      if (e.operation === "finalize_plan")
        return {
          behavior: "ask",
          message: `Design finalize_plan writes to claude.ai/design. ${pe}`,
          updatedInput: { ...d, __finalizePlanAskShown: !0 },
          localDisplayOnly: !0,
          decisionReason: {
            type: "safetyCheck",
            reason:
              "finalize_plan \u2014 the human is the path-review boundary; approval also grants prompt-free writes to these paths for 15 min",
            classifierApprovable: !1,
          },
        };
      if (y)
        return {
          behavior: "ask",
          message: `Design ${e.operation} writes to claude.ai/design.`,
          updatedInput: d,
          decisionReason: {
            type: "safetyCheck",
            reason: p
              ? "destructive \u2014 model-generated arguments"
              : "unknown operation \u2014 fail-closed to destructive write",
            classifierApprovable: !1,
          },
        };
      return {
        behavior: "ask",
        message: `Design ${e.operation} writes to claude.ai/design.`,
        updatedInput: d,
        decisionReason: {
          type: "safetyCheck",
          reason: "remote write \u2014 model-generated arguments",
          classifierApprovable: !1,
        },
      };
    },
    async call(e, t) {
      let n = t.toolState.get(wv),
        r = e.__consentAskCanReachUser ?? !1,
        d = r && ne(t);
      if (e.operation === "finalize_plan" && r) {
        let h = (e.arguments ?? {}).project_id;
        if (typeof h === "string" && h.length > 0) Ite(n, h);
      }
      if (Cte.has(e.operation)) {
        let h = (e.arguments ?? {}).project_id;
        if (typeof h === "string" && h.length > 0) (Ite(n, h), HSe(n, h), Xce(n, h), wSe(n, h));
      }
      let o = await HDe(t.credentials);
      if (!o.ok)
        throw new C(
          Ve(o, { isNonInteractiveSession: t.options?.isNonInteractiveSession }),
          "design_tool_auth_failed",
        );
      let p = o.accessToken,
        _ = e.__consentBitShown ?? null,
        y = e.__projectGrantAskShown ?? null,
        s = e.__projectGrantServerObserved === !0,
        u = (() => {
          if (e.operation !== "write_files" && e.operation !== "create_support_js") return null;
          let h = e.arguments ?? {};
          if (typeof h.plan_token === "string" && h.plan_token.length > 0) return null;
          let S = h.project_id;
          return typeof S === "string" && S.length > 0 ? S : null;
        })();
      if (
        e.operation === "copy_files" &&
        !(typeof e.arguments?.plan_token === "string" && e.arguments.plan_token.length > 0)
      )
        throw new C(
          "copy_files without a plan_token always requires per-batch approval \u2014 use finalize_plan declaring every destination in writes, then pass the returned plan_token.",
          "design_tool_exec_backstop_tokenless_copy",
        );
      if (u !== null) {
        let h = PKe(e.operation, e.arguments),
          S = e.__reservedTargetsAskShown === !0;
        if (h.outcome !== "pass" && !S)
          throw new C(
            `${e.operation} without a plan_token: this batch includes paths that always require per-batch approval \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token.`,
            "design_tool_exec_backstop_safety_trip",
          );
      }
      let g = t.abortController.signal,
        w = t.agentContext?.agentId ?? null,
        k;
      try {
        if (
          ((k = await D({})),
          e.operation === "finalize_plan" && e.__finalizePlanAskShown === !0 && d && U(t))
        ) {
          let h = e.arguments ?? {},
            S = k.isError !== !0 ? MWn(k.content) : null,
            f = (v) => (Array.isArray(v) ? v.filter((A) => typeof A === "string") : []);
          if (S !== null && typeof h.project_id === "string" && h.scope !== "project")
            DWn(n, S.token, {
              projectId: h.project_id,
              writes: f(h.writes),
              deletes: f(h.deletes),
              serverExpiresAtMs: S.expiresAtMs ?? void 0,
            });
        }
      } catch (h) {
        if (g.aborted && !(h instanceof tt)) throw new tt();
        if (h instanceof ae) {
          let S = await We(t.credentials, {
            isNonInteractiveSession: t.options?.isNonInteractiveSession,
            wasRetried: h.wasRetried,
          });
          if (g.aborted) throw new tt();
          throw new C(S, "design_tool_auth_401");
        }
        if (h instanceof Z && t.options?.isNonInteractiveSession)
          throw new Z({ isNonInteractiveSession: !0 });
        throw h;
      }
      return { data: k };
      async function D(h) {
        try {
          let S = await Se(n, e.operation, e.arguments, p, g, w, t.credentials);
          if (_ !== null && !g.aborted && S.isError !== !0) Lz(n, _, !0);
          if (u !== null && !g.aborted && S.isError !== !0 && ((y === u && !s) || !K9(n, u)))
            Rte(n, u);
          return S;
        } catch (S) {
          if (S instanceof X) {
            if ((Xce(n, S.projectId), wWn(n), h.grant === !0)) throw S;
            if (S.projectId !== y || s) {
              if (e.operation === "write_files" || e.operation === "create_support_js") {
                if (t.options?.isNonInteractiveSession === !0)
                  throw new C(
                    "Writing to this project needs a project write grant that is not currently active (it may have been revoked) \u2014 use finalize_plan with writes (and deletes if needed), then pass the returned plan_token. A durable project write grant can be approved from an interactive Claude Code session.",
                    "design_tool_needs_project_grant_non_interactive",
                  );
                throw new C(
                  "Writing to this project needs a one-time approval \u2014 retry the write: you will be shown the approval, or routed to the per-batch finalize_plan flow.",
                  "design_tool_needs_project_grant_not_shown",
                );
              }
              throw new C(
                "This operation needs a plan_token here \u2014 use finalize_plan (listing the destination paths in writes) and pass the returned plan_token. (It can run without one only under an existing project write grant, set up by a write_files to this project.)",
                "design_tool_needs_project_grant_ineligible_op",
              );
            }
            if (!d || !U(t))
              throw new C(
                "Writing to this project needs a one-time approval, which cannot be granted automatically in this permission mode \u2014 use finalize_plan with writes/deletes and pass the returned plan_token.",
                "design_tool_needs_project_grant_no_prompt",
              );
            try {
              await vWn(n, S.projectId, t.credentials);
            } catch (v) {
              if (ji(v).kind === "auth")
                throw new C(
                  "This session's credential cannot record the project approval \u2014 approve this project from an interactive Claude Code session (run the write there and accept the approval card), then retry here: this session picks the recorded approval up from the server.",
                  "design_tool_grant_mint_bearer_refused",
                );
              throw v;
            }
            return D({ ...h, grant: !0 });
          }
          if (!(S instanceof K)) throw S;
          if (h.consent === !0) throw S;
          let f = S.consent;
          if (f !== _)
            throw (
              Lz(n, f, !1),
              new C(
                `${kte(f)} The user hasn't granted this yet \u2014 ask them to retry (the prompt will show on the next call) or run /design consent.`,
                "design_tool_needs_consent_not_shown",
              )
            );
          if (!d)
            throw (
              Lz(n, f, !1),
              new C(
                `${kte(f)} The user hasn't granted this \u2014 run /design consent to grant it (it can't be approved automatically in this permission mode).`,
                "design_tool_needs_consent_no_prompt",
              )
            );
          return (await Tte(n, f, t.credentials), D({ ...h, consent: !0 }));
        }
      }
    },
    mapToolResultToToolResultBlockParam(e, t) {
      let n = Fe(e.content.flatMap(Pe)),
        r = e.isError
          ? n
              .flatMap((d) => (d.type === "text" ? [d.text] : []))
              .join(
                `
`,
              )
              .trim() || "(error with no message)"
          : null;
      return {
        tool_use_id: t,
        type: "tool_result",
        is_error: e.isError,
        content: r ?? (n.length > 0 ? n : "(empty result)"),
      };
    },
    renderToolUseMessage(e, { verbose: t }) {
      let r = (f, v = 80) => {
          let A = f.replace(/[\x00-\x1f\x7f-\x9f]+/g, " ");
          return A.length > v ? A.slice(0, v).trimEnd() + "\u2026" : A;
        },
        d = e.arguments;
      if (!d || Object.keys(d).length === 0) return "";
      if (!t) {
        let f = {};
        for (let [v, A] of Object.entries(d))
          if (Array.isArray(A)) {
            let j = v === "messages" ? "message" : v === "validators" ? "validator" : "file";
            f[j] = (f[j] ?? 0) + A.length;
          }
        return Object.entries(f)
          .map(([v, A]) => `${A} ${A === 1 ? v : v + "s"}`)
          .join(", ");
      }
      if (!Object.hasOwn(G, e.operation ?? "") && !Object.hasOwn(W, e.operation ?? "")) {
        let f = b(d) ?? "";
        return `${r(e.operation ?? "")} (${E(L(f))} of arguments not shown \u2014 operation unknown to this client version)`;
      }
      let o = (f, v) => {
          if (typeof f === "string") return f.length > 80 ? `${r(f)} (${E(L(f))})` : r(f);
          if (typeof f === "object" && f !== null) {
            let j = f,
              F = L(b(f) ?? "");
            if (typeof j.path === "string") {
              let R = typeof j.data === "string" ? j.data : "",
                P = ["encoding", "local_path", "if_match"].filter(
                  (H) => typeof j[H] === "string" && j[H],
                ),
                I = `${r(j.path)} (${E(F)})${P.length ? ` [+${P.join(",")}]` : ""}`;
              return v > 0 && R ? `${I}: ${r(R, v)}` : I;
            }
            if (typeof j.content === "string") {
              let R = typeof j.role === "string" ? j.role : "?",
                P = `${r(R, 12)} (${E(F)})`;
              return v > 0 && j.content ? `${P}: ${r(j.content, v)}` : P;
            }
            if (typeof j.src === "string" || typeof j.dest === "string")
              return `${r(String(j.src ?? "?"), 40)} \u2192 ${r(String(j.dest ?? "?"), 40)}${j.src_project_id ? ` [from ${r(String(j.src_project_id), 24)}]` : ""}${typeof j.if_match === "string" && j.if_match ? " [+if_match]" : ""}${F > 256 ? ` (${E(F)})` : ""}`;
          }
          let A = b(f) ?? String(f);
          return `${r(A)} (${E(L(A))})`;
        },
        p = (f) =>
          typeof f === "string"
            ? r(f, 40)
            : typeof f === "object" && f !== null
              ? r(String(f.path ?? f.dest ?? f.role ?? "?"), 40)
              : "?",
        _ = (f) => {
          if (Array.isArray(f) && f.length > 0) {
            let A = f.slice(0, 20),
              j = t ? Math.max(0, Math.min(200, Math.floor(1800 / A.length) - 95)) : 0,
              F =
                t &&
                j === 0 &&
                A.some((I) => {
                  if (typeof I !== "object" || I === null) return !1;
                  let H = I;
                  return (
                    (typeof H.data === "string" && H.data) ||
                    (typeof H.content === "string" && H.content)
                  );
                }),
              R = f.slice(20),
              P =
                R.length > 0
                  ? `, +${R.length} more: ${R.slice(0, 10).map(p).join(", ")}${R.length > 10 ? ", \u2026" : ""}`
                  : "";
            return (
              `[${f.length} ${f.length === 1 ? "entry" : "entries"}: ` +
              A.map((I) => o(I, j)).join(", ") +
              P +
              "]" +
              (F ? " (content previews omitted)" : "")
            );
          }
          let v = b(f) ?? String(f);
          return v.length > 80 ? `${r(v)} (${E(L(v))})` : r(v);
        },
        y = Object.entries(d).sort(
          ([, f], [, v]) => Number(Array.isArray(f)) - Number(Array.isArray(v)),
        ),
        s = y.filter(([, f]) => Array.isArray(f)),
        u = s.length >= 2 ? ` (${s.map(([f, v]) => `${r(f)}: ${v.length}`).join(", ")})` : "",
        g = y.map(([f, v]) => `${r(f)}: ${_(v)}`).join(", "),
        w = b(d) ?? "",
        k = L(w),
        h = `${k > 256 ? ` (${E(k)} total)` : ""}${u} ${g}`.trimStart(),
        S = t ? 2000 : 1500;
      return h.length > S ? h.slice(0, S - 1).trimEnd() + "\u2026" : h;
    },
  }),
  ze = /^image\/(png|jpeg|gif|webp)$/,
  we = 130000,
  se = 130000;
function Pe(e) {
  if (e.type === "text" && typeof e.text === "string") return [{ type: "text", text: e.text }];
  if (
    e.type === "image" &&
    typeof e.data === "string" &&
    typeof e.mimeType === "string" &&
    ze.test(e.mimeType)
  ) {
    if (e.data.length > we)
      return [
        {
          type: "text",
          text: `[render_preview image omitted \u2014 ${Math.round(e.data.length / 1024)}KB exceeds ${Math.round(we / 1024)}KB cap]`,
        },
      ];
    return [{ type: "image", source: { type: "base64", media_type: e.mimeType, data: e.data } }];
  }
  return [{ type: "text", text: b(e) ?? "" }];
}
function He(e) {
  return e.type === "image" && e.source.type === "base64"
    ? e.source.data.length
    : e.type === "text"
      ? e.text.length
      : 0;
}
function Fe(e) {
  let t = se,
    n = [],
    r = 0,
    d = 0;
  for (let o of e) {
    let p = He(o);
    if (p <= t) ((t -= p), n.push(o));
    else (r++, (d += p));
  }
  if (r > 0)
    n.push({
      type: "text",
      text: `[ClaudeDesign result truncated \u2014 ${r} block(s) (${Math.round(d / 1024)}k chars) omitted; aggregate over ${Math.round(se / 1024)}k-char cap]`,
    });
  return n;
}
class K extends Error {
  consent;
  constructor(e) {
    super("Claude Design requires consent");
    this.consent = e;
    this.name = "DesignNeedsConsentError";
  }
}
function xe(e) {
  if (e == null || typeof e !== "object" || e.error !== "needs_consent") return null;
  let t = e.consent;
  return RKe(t) ? t : null;
}
class X extends Error {
  projectId;
  constructor(e) {
    super("Claude Design requires a project write grant");
    this.projectId = e;
    this.name = "DesignNeedsProjectGrantError";
  }
}
function Ne(e) {
  if (e == null || typeof e !== "object" || e.error !== "needs_project_grant") return null;
  let t = e.project_id;
  if (typeof t !== "string" || t.length === 0 || t.length > LKe - 2 || !/^[A-Za-z0-9._-]+$/.test(t))
    return null;
  return t;
}
class Z extends C {
  constructor(e) {
    super(
      e?.isNonInteractiveSession === !0
        ? "Claude Design rejected this session's claude.ai credential (HTTP 403): it does not carry Claude Design access, and /design login cannot run in this non-interactive session. Ask the user to run /design login once from an interactive Claude Code session on this machine \u2014 non-interactive runs here then reuse that authorization. (CI runners and hosted sessions have no interactive session; Claude Design is not reachable from those without a stored /design login credential.)"
        : "Claude Design rejected this session's claude.ai credential (HTTP 403): it does not carry Claude Design access. Run /design login to authorize Claude Design, then retry.",
      "design_tool_needs_design_scopes",
    );
    this.name = "DesignNeedsScopesError";
  }
}
var Le = 64;
function ve(e, t, n) {
  if (
    (e.servedCatalogHashes.delete(t),
    e.servedCatalogHashes.set(t, n),
    e.servedCatalogHashes.size > Le)
  ) {
    let r = e.servedCatalogHashes.keys().next().value;
    if (r !== void 0) e.servedCatalogHashes.delete(r);
  }
}
function Be(e) {
  let t = e.replace(/\s+/g, " ").trim(),
    n = t.indexOf(". "),
    r = n > 0 ? t.slice(0, n + 1) : t;
  return r.length > 140 ? le(r, 140).trimEnd() + "\u2026" : r;
}
async function Se(e, t, n, r, d, o, p) {
  try {
    let _ = null;
    if (t === M2 || ue(t)) _ = await Ue(e, r, d, p);
    if (t === M2) {
      let w = (_ ?? []).map((f) => ({
          name: f.name,
          description: f.description,
          inputSchema: f.inputSchema,
        })),
        k = b({ tools: w }) ?? "",
        D = Dn(k),
        h = n?.full;
      if (
        !(h !== void 0 && h !== !1 && h !== "false" && h !== 0 && h !== "") &&
        o !== null &&
        e.servedCatalogHashes.get(o) === D
      ) {
        ve(e, o, D);
        let f = {
          catalog_unchanged: !0,
          catalog_hash: D,
          note:
            `The operation catalog is unchanged since the earlier "${M2}" result in this conversation (hash ${D}) \u2014 full descriptions and argument schemas are in that result. ` +
            `If it is no longer in context, call ${J8}({operation: "${M2}", arguments: {full: true}}) for the full catalog.`,
          operations: w.map((v) => ({ name: v.name, summary: Be(v.description) })),
        };
        return { operation: t, content: [{ type: "text", text: b(f) }] };
      }
      if (o !== null) ve(e, o, D);
      return { operation: t, content: [{ type: "text", text: k }] };
    }
    if (_ && N(t) === void 0)
      return {
        operation: t,
        content: [
          {
            type: "text",
            text: `Unknown Claude Design operation "${t}". Call ${J8}({operation: "${M2}"}) to see the available operations.`,
          },
        ],
        isError: !0,
      };
    let y = (w) =>
        ee(
          { jsonrpc: "2.0", id: 1, method: "tools/call", params: { name: t, arguments: n } },
          r,
          w,
          d,
          p,
        ),
      s = await Q(e, r, d, p),
      u = await y(s);
    if (u.status === 404 && s) (ke(e), (u = await y(await Q(e, r, d, p))));
    if (u.status === 403) {
      let w = xe(u.data);
      if (w !== null) throw new K(w);
      let k = Ne(u.data);
      if (k !== null) throw new X(k);
    }
    if (u.status < 200 || u.status >= 300)
      throw new C(
        `Claude Design ${t} failed: HTTP ${u.status} ${B(b(u.data) ?? "", r)}`,
        "design_tool_http_error",
      );
    let g = u.data;
    if (g.error)
      return {
        operation: t,
        content: [
          {
            type: "text",
            text: B(`${g.error.message}${g.error.data ? ` \u2014 ${b(g.error.data)}` : ""}`, r),
          },
        ],
        isError: !0,
      };
    return { operation: t, content: g.result?.content ?? [], isError: g.result?.isError };
  } catch (_) {
    if (d.aborted) throw new tt();
    if (_ instanceof C || _ instanceof K || _ instanceof X) throw _;
    if (Me(_))
      throw new C(
        `Claude Design ${t} was cancelled because a concurrent Claude Design call was interrupted mid-handshake (the first calls of a session share one initialize/discovery round-trip). Retry the operation.`,
        "design_tool_shared_handshake_cancelled",
      );
    throw new C(B(l(_), r), "design_tool_request_failed");
  }
}
function Me(e) {
  if (typeof e !== "object" || e === null) return !1;
  let t = e;
  return t.name === "CanceledError" || t.name === "AbortError" || t.code === "ERR_CANCELED";
}
function Ue(e, t, n, r) {
  if (e.discoverInFlight) return e.discoverInFlight;
  let d = qe(e, t, n, r);
  return (
    (e.discoverInFlight = d),
    d.finally(() => {
      if (e.discoverInFlight === d) e.discoverInFlight = null;
    })
  );
}
async function qe(e, t, n, r) {
  let d = (_) => ee({ jsonrpc: "2.0", id: 1, method: "tools/list", params: {} }, t, _, n, r),
    o = await Q(e, t, n, r),
    p = await d(o);
  if (p.status === 404 && o) (ke(e), (p = await d(await Q(e, t, n, r))));
  if (p.status < 200 || p.status >= 300 || !Array.isArray(p.data.result?.tools))
    throw new C(`Claude Design discovery failed: HTTP ${p.status}`, "design_tool_discovery_failed");
  return ce(p.data.result.tools);
}
function ke(e) {
  ((e.cachedSessionId = null),
    (e.initialized = !1),
    (e.initializeInFlight = null),
    (e.discoverInFlight = null));
}
async function Q(e, t, n, r) {
  if (e.initialized) return e.cachedSessionId;
  if (e.initializeInFlight) return e.initializeInFlight;
  let d = (async () => {
    let o = await ee(
      {
        jsonrpc: "2.0",
        id: 0,
        method: "initialize",
        params: {
          protocolVersion: "2025-03-26",
          capabilities: {},
          clientInfo: { name: "claude-cli-design-tool", version: "1" },
        },
      },
      t,
      null,
      n,
      r,
      "text",
    );
    if (o.status < 200 || o.status >= 300)
      throw new C(
        `Claude Design initialize failed: HTTP ${o.status}`,
        "design_tool_initialize_failed",
      );
    let p = o.response.headers["mcp-session-id"];
    return (
      (e.cachedSessionId = typeof p === "string" && p ? p : null),
      (e.initialized = !0),
      e.cachedSessionId
    );
  })();
  e.initializeInFlight = d;
  try {
    return await d;
  } finally {
    if (e.initializeInFlight === d) e.initializeInFlight = null;
  }
}
class ae extends C {
  wasRetried;
  constructor(e = !1) {
    super(
      "Claude Design authentication failed (HTTP 401). The claude.ai credential is missing or expired \u2014 run /login, or /design login for a separate design credential.",
      "design_tool_auth_401",
    );
    ((this.name = "DesignAuth401Error"), (this.wasRetried = e));
  }
}
var be = 5000;
async function Ge(e, t, n) {
  let r;
  if (M() && n !== void 0) r = await oa(n);
  else r = tn();
  if (e === r?.accessToken && Boolean(r?.refreshToken)) {
    if ((await Promise.race([hm(e, n).catch(() => !1), re(be, t)]), t.aborted)) return null;
  }
  let d = await Promise.race([HDe(n).catch(() => null), re(be, t).then(() => null)]);
  return d?.ok === !0 && d.accessToken !== e ? d.accessToken : null;
}
async function We(e, t) {
  let n = t?.isNonInteractiveSession === !0,
    r =
      t?.wasRetried === !0
        ? "Claude Design authentication failed (HTTP 401): a freshly refreshed credential was also rejected \u2014 likely a server-side access problem with this account or credential rather than simple expiry."
        : "Claude Design authentication failed (HTTP 401): the credential was rejected and an automatic refresh did not produce a new one.",
    d;
  if (M() && e !== void 0) d = await oa(e);
  else d = tn();
  let o = !!d?.accessToken && !!d.refreshToken && d.scopes?.includes("user:design:read") === !0;
  if (!o && (await EWn(e)))
    return `${r} The design credential (from /design login) is expired or revoked${n ? ", and /design login requires an interactive terminal \u2014 re-authenticate outside this session" : " \u2014 run /design login to re-authenticate"}.`;
  if (!o && d?.accessToken && !d.refreshToken) {
    let { source: p } = M() && e !== void 0 ? await d3t(e) : Jl();
    if (a.CLAUDE_CODE_REMOTE_SESSION_ID)
      return `${r} This remote session's credential is injected and rotated by the session host \u2014 it usually self-heals within minutes. Retry shortly; if this persists, the host session needs attention.`;
    if (
      (p === "CCR_OAUTH_TOKEN_FILE" || p === "CLAUDE_CODE_OAUTH_TOKEN_FILE_DESCRIPTOR") &&
      (!FZ() || Nc())
    )
      return `${r} This session authenticates with an OAuth token injected by the CCR host, which has no refresh token \u2014 it cannot self-heal and has likely expired or been revoked. The credential comes from the host session; check or restart it there.`;
    if (
      p === "CLAUDE_CODE_OAUTH_TOKEN" ||
      (p === "ANTHROPIC_AUTH_TOKEN" && Boolean(a.CLAUDE_CODE_OAUTH_TOKEN))
    )
      return `${r} This session authenticates with the CLAUDE_CODE_OAUTH_TOKEN environment variable, which has no refresh token \u2014 it cannot self-heal and has likely expired or been revoked. Mint a fresh token with \`claude setup-token\` and restart the session with it${n ? "" : ", or unset it and run /login"}.`;
  }
  if (n)
    return `${r} The claude.ai credential is expired or revoked, and /login requires an interactive terminal \u2014 re-authenticate outside this session.`;
  return `${r} Run /login, or /design login for a separate design credential.`;
}
async function ee(e, t, n, r, d, o, p = !1) {
  if (!KA(Jt().BASE_API_URL))
    throw Error(
      "Claude Design is only reachable from api.anthropic.com; the current OAuth base URL is not on the first-party allowlist.",
    );
  let _ = await bt.post("/v1/design/mcp", e, {
    auth: "none",
    headers: {
      ...$e(t),
      Accept: "application/json, text/event-stream",
      "X-Anthropic-Client": "claude-cli-design-tool",
      ...(n && { "Mcp-Session-Id": n }),
    },
    timeout: 60000,
    maxContentLength: 16 * se,
    validateStatus: () => !0,
    signal: r,
    ...(o && { responseType: o }),
  });
  if (!_.ok) throw new C(`Claude Design unavailable: ${_.reason}`, "design_tool_policy_refused");
  if (_.status === 401) {
    if (!p && !r.aborted) {
      let s = await Ge(t, r, d);
      if (s !== null && !r.aborted)
        try {
          let u = await ee(e, s, n, r, d, o, !0);
          if (typeof u.data === "string") {
            if (u.data.includes(s)) u.data = B(u.data, s);
          } else if (u.data !== null && u.data !== void 0) {
            let g = b(u.data);
            if (g !== void 0 && g.includes(s)) u.data = Y(B(g, s));
          }
          return u;
        } catch (u) {
          if (u instanceof Error)
            try {
              u.message = B(u.message, s);
            } catch {}
          throw u;
        }
    }
    if (!p && r.aborted) throw new tt();
    throw new ae(p);
  }
  if (_.status === 403) {
    let s = _.data;
    if (typeof s === "string")
      try {
        s = Y(s);
      } catch {
        s = null;
      }
    if (s !== null && typeof s === "object" && s.error === "needs_design_scopes") throw new Z();
  }
  let y = _.response.headers["content-type"];
  if (typeof y === "string" && y.toLowerCase().startsWith("text/event-stream"))
    throw new C(
      "Claude Design returned a text/event-stream response; this client only handles JSON.",
      "design_tool_sse_unsupported",
    );
  return _;
}
function Ve(e, t) {
  let n = e.detail ? ` (${e.detail})` : "";
  switch (e.reason) {
    case "needs_design_login":
      if (t?.isNonInteractiveSession)
        return "Claude Design needs a claude.ai credential, and /design login cannot run in this non-interactive session. Ask the user to run /design login once from an interactive Claude Code session on this machine \u2014 non-interactive runs here then reuse that authorization. (CI runners and hosted sessions have no interactive session; Claude Design is not reachable from those without a stored /design login credential.)";
      return `No design-capable credential${n}. Run /design login to authorize Claude Design.`;
    case "design_refresh_failed":
      return `Design token refresh failed${n}. Retry shortly${t?.isNonInteractiveSession ? "" : ", or run /design login"}.`;
    case "wrong_provider":
      return "Claude Design is only available with claude.ai authentication. It is not supported through Bedrock, Vertex, or other third-party providers.";
    case "essential_traffic_only":
      return "Claude Design is disabled because nonessential network traffic is restricted.";
  }
}
czn({
  isEnabled: HWn,
  createObserver: (e, t, n) =>
    e.kind === "design_project_grant" ? kWn(t.get(wv), e.projectId, n) : null,
});
export { Dt as DesignTool, De as operationLabel };
